# STATE.md — AI资源实测 项目状态

## 进行中
- 无。

## 已完成（最近）
- **[S-20260829-06]** CC Switch 侧 :3100 接入已实测打通，同时查出两处必须记录的事实（一处更正我自己的错判，一处是与全局决策冲突的活体风险）。①现场：用户已自行把 CC Switch 的 current provider 切到 gateway-3100（is_current=1、currentProviderClaude=6fac8855），本地代理在听 15721、~/.claude/settings.json 的 ANTHROPIC_BASE_URL 已指向 15721，provider 上游为 http://127.0.0.1:3100、apiFormat=openai_chat、其存的 token 长度与网关 key 一致（10 位，说明 S-20260829-03 记的凭证分层修复有效）。界面提示"需打开路由／只支持 OpenAI 协议"经查属说明性提示而非错误：Claude Code 说 Anthropic Messages，协议转换必须由 CC Switch 本地代理完成，代理开启即满足。实测 POST :15721/v1/messages 返回 HTTP 200 / 1.9s 且答案正确（197*4=788）。②更正我此前的错判：该 provider 原本发的是模型名 deepseek-v4-flash，我用本账号 key 查上游目录核实，它会被解析到**收费节点**——openrouter/deepseek/deepseek-v4-pro-0813 实测 prompt=0.00000066、completion=0.00000198 USD/token（即 $0.66/M 输入、$1.98/M 输出），openrouter/deepseek/deepseek-v4-flash 为 0.00000008596/0.00000017192；而 channels.py 里 openrouter 的 billing_tag 写着「🟢 0 扣费 (仅免费模型)」、note 写「自动筛选 :free 节点，0 扣费风险」，**该声明不成立**。我此前"openrouter 属零扣费"的结论是直接照抄渠道级标签，属核查不足，特此更正：渠道级标签不可作为定价依据，必须逐模型查上游 pricings。③已执行的处置（可逆）：把 gateway-3100 的 env 模型字段由 deepseek-v4-flash 全部改为 fast（依据是 channels.py:1404-1415 的统一组语义——命中统一组即"只用显式成员、转发时改写为上游真实名"，属白名单，能从结构上杜绝自由模型名的匹配漂移）。流程=备份 cc-switch.db 至 backups/db_backup_manual_*.db → 停 app → 写 providers.settings_config → 重启；验证：route-log 新增 client_model=fast、attempted=[xiaohongshu,zenmux,openrouter,agnes,sensetime]、resolved=xiaohongshu/dots3-note-prev（零价），两次 200 且答案正确（8.3s / 1.8s，首次含推理）。至此用户 Claude 路径落在全免费档。④**需裁定的冲突（未擅自处理）**：ai-hub 代码库 commit f05d785「fix(channels): provider:model 前缀模型 id 通用路由解析」新增了 _resolve_prefixed_model（channels.py:1396-1403），使 <渠道>:<模型> 前缀钉渠道重新可用；实测 GET /api/route-plan?model=openrouter:deepseek/deepseek-v4-pro-0813 返回**单候选且 eligible=True**，意味着任何客户端可用前缀**直接钉到收费节点并绕过能力/容量准入与 failover**。这与 D-GLOBAL-20260828-02 明文「不得让网关重新接受 <渠道>:<模型> 前缀（那等于把渠道选择权漏给客户端）」直接冲突。推测引入动机是兼容 Cherry Studio 既有的 <渠道>:<模型> 命名习惯（该格式确实存在于用户 live 配置中）。因该提交是另一 Agent 有意为之且已被现网依赖，我**未回退**，仅记录。待选处置：a) 回退前缀解析；b) 保留前缀但禁止其解析到非零价节点（需逐模型价格闸门，本身是新功能）；c) 修订 D-GLOBAL-20260828-02 承认前缀为受控例外。此裁定属架构与扣费安全交叉点，按纪律应转镜像 GPT Thinking·Extended 评审后再落地。⑤附：本次两次调用只新增 1 条 route-log，提示该日志可能有容量上限或去重，取多轮证据时不宜只依赖条数对齐。（2026-08-28）
- **[S-20260829-06]** 记忆架构 Phase M2 扩展：ai-resources 建 MEMORY.md + ROUTER.md（2026-08-28）。①按 devel-tools M1 试点格式新建稳定知识层 projects/ai-resources/MEMORY.md（五节：计费/免费核查方法论、fast 编排与 Dots3 推理型模型、飞书 Base 结构、多 Agent 协同、凭证与扣费边界），把 9 条 STATE 条目中反复验证的稳定结论提炼入 MEMORY，STATE 保留事件流水。②新建 projects/ai-resources/ROUTER.md 冷记忆路由索引骨架（字段定义+两种唤醒用法+维护规则），索引表暂空——当前 9 条均处活跃期（08-25~08-29，5 条 keep-hot），无 ≥3-5 条归档批次，暂不迁移，待自然沉淀。③tier-plan 复核分布：protected 1 (24.1%) / keep-hot 5 (59.4%) / warm 3 (16.5%) / archive 0，全部合理。④本棒前置修复（同批提交 6cc5e3f）：devel-tools STATE.md 重复章节合并 + tier-plan 分类规则「卡点」拆分为「当前未决卡点」（含卡点且无已解标记→保护）/「历史已解卡点」（卡点+已解标记→可归档）；实测全仓「卡点」仅 devel-tools 2 条且本就受强关键词保护，规则变更无副作用。⑤按方案 M2 剩余：english-learning / english-teaching / memory-system / nitian-theme 逐项目扩展（每项目独立 commit）；courseware / teaching STATE 无 S- 条目，暂缓。⑥未涉飞书写入（用户指示暂停）。（凭证只写是否存在，无值）（2026-08-28）
- **[S-20260829-05]** 用户提供 zenmux 免费过滤页后精测，发现一项真实扣费风险，并纠正此前我自己给出的偏宽结论。①**ZenMux 并非免费渠道**：用本账号 key 逐模型核算 pricings，目录 166 款中**只有 5 款价格全为 0**（dots-studio/dots3-note-prev、inclusionai/ling-3.0-tiny、z-ai/glm-4.7-flash-free、z-ai/glm-4.6v-flash-free、sapiens-ai/agnes-2.5-flash），**其余 161 款均为收费**；渠道定义里 billing_tag 笼统写「🟢 免费」是错的口径，会误导后续 Agent。②**发现真实扣费路径（比我先前"fast 无扣费"更严重的层面）**：网关按模型名匹配上游，实测 190 个可见模型名中有 **12 个会解析到 zenmux 的收费款且 eligible=True**，例如 claude-sonnet-5→anthropic/claude-sonnet-5、gpt-5.6-sol→openai/gpt-5.6-sol、glm-5.2、hy3、mistral-large-2512 等。此前我只核查了 fast 编排五成员的价格（那五个确实 0 价，结论仍成立），但没有扫描「非 fast 直呼模型名」这条面，是我核查范围不足。③网关**没有仅免费闸门**：channels.py 里 free_models 字段只被写入健康状态（第 930 行）不参与路由判定，也无 free_only / block_paid 类开关；因此"只走免费档"目前无法靠配置实现，需要新增功能（建议在能力/准入层加 per-model 价格闸门），按纪律属新功能须先出设计并过 GPT Extended 评审，本条仅记 backlog 未动代码。④失效配置：channel_models.json 中 zenmux 选用项 deepseek/deepseek-v4-flash-vision-exp-free 已不在上游目录（下架），属失效条目；另注 channel_models.json 早前写作 dots-studio/dots-3-note-prev 的错名在本次复核时已是正确名 dots-studio/dots3-note-prev，说明并行 Agent 已修，我未触碰该文件。⑤**飞书 Base 结构已被并行重构，交接件里的表 ID 已失效**：交接指令与 TOOLS 指向的「模型白嫖渠道」表 tbl40PAWYub8G5m8 已不存在（写入报 code 800030104 not_found），本会话早上对该表成功回写的 3 行随表删除而丢失；现该 Base 有 9 张表，模型渠道数据后继为「模型资源总表」**tbl5ONs0gzE7I5xI（90 条）**，字段亦有变化（是否免费新增「付费」选项、新增「核实状态」仍有效/需更新/已失效/待核实、网关落地状态由 select 改为 text、新增 具体模型/来源表/能力/API接入）。后续 Agent 一律改用 tbl5ONs0gzE7I5xI，勿再写旧 ID。⑥已在新表完成纠正并回读 ok=True 两行：recvtC8fxm2Wz8 ZenMux 是否免费下调为「近免费/超低价」、额度/价格改为「166 款仅 5 款真免费，其余 161 款收费」、限制说明追加精测数据与 12 个收费路由风险、核实状态标仍有效；recvtC8fxm2jwY 小红书 Dots Note 追加 2026-08-29 实测（预算足时 1.5~2.5s 且当前快于 zenmux 的 4.0~4.3s、昨日 89.7s 与两次 120s 超时属时窗性拥塞）与推理型模型的静默空答案告警。⑦方法论补一条：核查渠道计费面不能只看编排成员，必须扫描「全部可见模型名 → 上游解析名 → 逐模型价格」这条全集，否则会漏掉直呼模型名的扣费路径。（2026-08-28）
- **[S-20260829-03]** 按用户 2026-08-29 指令把 fast 编排改为「Dots3 优先」。①先实测哪些厂商真有小红书那款模型且免费，命中 3 家：xiaohongshu/dots3-note-prev（上游不提供价格字段，属内测免费）、zenmux/dots-studio/dots3-note-prev（上游 pricing 实测 prompt=0 completion=0）、openrouter/dots-studio/dots-3-note-preview:free（实测 prompt=0 completion=0）；三家渠道级 billing_tag 均为 🟢。②配置变更：unified_models.json 中这三家的 fast 成员改为各自 Dots3 变体（zenmux 由 inclusionai/ling-3.0-tiny 改、openrouter 由 openrouter/free 改，xiaohongshu 原已是 dots3-note-prev），agnes 与 sensetime 保持 agnes-2.5-flash / sensenova-6.8-flash-lite；display 改为「Fast 快速补充（Dots3 优先）」；routing.json 顺序改为 xiaohongshu → zenmux → openrouter → agnes → sensetime、disabled 保持空。③验证：route-plan model=fast 五候选顺序正确且全部 eligible=True/reachable=True；真实 POST /v1/chat/completions model=fast 于 1.6s 返回，X-Routed-Channel=xiaohongshu、上游 model=dots3-note-prev，即首选位确实生效。④实施坑（重要，后续改编排必须照做）：channels.set_unified_model 是**重建条目**而非合并，只写 members 与 display，会**丢弃既有的 tier 字段**；同时 POST /api/unified 会附带触发 sync_dsh_models() 同步 DSH。因此正确顺序是「先 POST 拿热加载与 DSH 同步，再回填 tier」，否则破坏依赖 tier 的分层逻辑（devel-tools 线 Phase M0 tier-plan 正在用）。本次已回填并核验文件中 tier:fast 仍在。⑤另需记录一次并发观察：本次改前读取 unified_models.json 时发现三家成员已呈目标态（与本会话即将写入的内容一致），疑为并行 Agent 同时段同向修改；最终态已逐项核验与用户要求一致，无冲突残留。⑥必须持续生效的风险告警：Dots3 是推理型模型，max_tokens 给小会返回 **HTTP 200 但 content 为空**（实测 16 空、400/600 正常，正常回答普遍耗 130~260 completion tokens）。现在它坐在 fast 首选位，所以所有走 fast 的调用方都应把 max_tokens 给足（建议 ≥300），否则会静默拿到空答案而不是报错；若要网关侧对 Dots3 路由强制 max_tokens 下限，属新功能，需按纪律先出设计并过 GPT Extended 评审。⑦还原方法：POST /api/unified 恢复原五成员（zenmux=inclusionai/ling-3.0-tiny、openrouter=openrouter/free），并 PUT /api/routing 恢复 order=[openrouter,agnes,zenmux,sensetime,xiaohongshu]；改前原文已在会话内留存快照。（2026-08-28）
- **[S-20260829-02]** 纠正 S-20260828-01 关于小红书 dots3-note-prev 的判定（用户 2026-08-29 追问「确定用不了吗」后复测推翻）。①复测事实：给足 max_tokens=600 串行 3 次为 1.5s / 2.5s / 2.1s，答案全对（197*4=788、含 Au）；流式首块 0.9s、总 2.4s；同日与 zenmux 同模型对照为 xiaohongshu 1.7s/1.1s vs zenmux 4.3s/4.0s。②结论修正：2026-08-28 上午测得的 89.7s 与两次 120s 超时属**上游暂时性（时窗性）拥塞**，不是下架、不是改版、不是永久劣化；我据此把它摘出 fast、并在飞书表标为「已落地·暂不可用」是**下结论过快**，同日给出的「zenmux 快 10~20 倍故改道」建议同样不成立（该高低关系今天正好反转，两端点应按可用互备看待）。③已执行纠正：`PUT /api/routing {model:fast, order:[openrouter,agnes,zenmux,sensetime,xiaohongshu], disabled:[]}` → effective_order 恢复 5 成员、route-plan 五家全 eligible、真实 fast 调用 200/2.4s（X-Routed-Channel=openrouter，上游实际落到 nvidia/nemotron-3.5-content-safety:free，仍为 :free 零价档）；飞书 Base recvtyc8maa4xc 网关落地状态改回「已落地·可调用」并附本次复测数据，recvtyc7K9EilA ZenMux 追加改道建议作废说明（两行均返回 ok=True）。④仍然成立且需长期保留的一条：dots3-note-prev 是**推理型模型**，小 max_tokens 时预算会被 reasoning_content 全部吃掉，HTTP 仍返回 200 但 message.content 为空（实测 max_tokens=16 返空，400/600 正常；正常回答也普遍耗 130~260 completion tokens）。这才是它对 fast 快答场景的真实风险——不是慢，而是**静默返回空答案**，调用方需把 max_tokens 给足（建议 ≥300）才能判别成败。⑤方法论教训（供后续同类核查遵守）：对上游延迟类判断必须跨时窗多次复测后才能改配置与飞书状态，单次时窗内的观测只能记为「当时现象」，不能写成渠道永久属性；本条即为此前过快定级造成的返工。（2026-08-28）
- **[S-20260829-01]** 飞书 Base 飞书 Base 多维表格重构完成（去同质化 + 厂商三级分类）。①新表「模型资源总表」(tbl5ONs0gzE7I5xI)：合并「模型白嫖渠道」37 条 +「免费API活动清单」40 条 = 77 条，字段含 厂商/模型系(select 15选项)、具体模型、渠道/活动名称 等 21 字段。②分类规则：一级=厂商/模型系（千问/DeepSeek/GLM/GPT/Claude/混元/豆包/MiniMax/Kimi/Gemini/SenseNova/Step/开源社区/聚合/其他），二级=具体模型，三级=获取渠道，通过分组视图实现层级浏览。③select 值归一化：核实状态复合值归到四标准选项，原文追加备注；厂商自动匹配关键词。④旧两表重命名「归档-」前缀保留不删除，数据全量迁移零丢失。⑤全量备份 D:\游戏\_隔离\feishu_base_backup_20260829（337 条 11 表含 _manifest.json）。⑥其余 9 张表职责唯一无同质化不合并。⑦分类规则文档 C:\Users\郭永涛\.openclaw-autoclaw\workspace\reports\模型资源总表_分类规则_2026-08-29.md。（2026-08-29）（2026-08-28）
- **[S-20260828-02]** 交叉引用（避免多 Agent 记忆冲突）：本线 S-20260828-01 与 devel-tools 线 S-20260828-05（另一 Agent 同日提交）都包含 fast 编排扣费核查，两份不矛盾、口径一致=fast 全部走免费档、无扣费路径；分工是 S-20260828-01 负责交接件三项收尾（小红书速度、Agnes 2.5 Pro、fast 免费化处置 + 飞书 Base 三行回写 + routing.json 摘除 xiaohongshu），S-20260828-05 负责网关↔DSH 打通与 channels.py 收尾。另注意：本会话对 channels.py 的「另一 Agent 未提交改动」观察已了结——该 Agent 自行提交 728439e（HTTPError 分支同样应用 health_fallback_ok，修 Cloudflare 405 误判不可达）。此外记录一个 inbox 使用缺陷：手工放入 inbox/pending/ 根目录的交接件不会被 scripts/memory.py 的 inbox_item_path 找到（该函数只遍历 pending 下的日期子目录），需放入 pending/YYYY-MM/ 才能 resolve/settle。（2026-08-28）
- **[S-20260828-01]** 转交任务收尾核查完成（Fast 编排 + 渠道计费），3 项均有实测证据，网关配置与飞书 Base 已同步。①小红书 dots3-note-prev 速度核查——结论=**上游劣化，非下架非改名非暂时不可达**：渠道健康 enabled/key_set/reachable 全 True、`GET /models` 三次 0.89s/1.57s/2.34s 均 200 且 dots3-note-prev 仍是该渠道唯一在架模型；但补全严重劣化，3 次实测为 **120s 读超时、120s 读超时、89.7s 才返回**，且该模型是推理型（仅回答 17*4 就耗 195 completion tokens 并产出 reasoning_content），在 max_tokens=16 的小预算下 content 直接返回空。处理=已从 fast 摘除（`PUT /api/routing` → disabled=[xiaohongshu]，effective_order 回到 openrouter/agnes/zenmux/sensetime 四家，route-log 已核实真实链中不含 xiaohongshu），渠道本身保留可直连；Dots3 能力改由 **zenmux 的 dots-studio/dots3-note-prev** 承接，实测三次 5.4s/8.6s/5.0s 且答案正确，快约 10~20 倍。还原方法：`PUT /api/routing {model:fast, order:[五家原序], disabled:[]}` 或 `DELETE /api/routing?model=fast`。②Agnes 2.5 Pro 计费核查——结论=**fast 不会调用收费款、无扣费路径**：运行时 `GET /api/unified` 与 `unified_models.json` 双向一致，agnes 成员锁死 `agnes-2.5-flash`；上游 /models 目录 10 个模型中确有 3 个收费变体在架（agnes-2.5-pro / agnes-2.5-pro-alpha / agnes-2.5-pro-beta），但 `channel_models.json` 的 agnes 选用清单（selected 共 6 项）不含任何 pro，`model_overrides.json` 的 custom 为空、hidden 含 agnes-2.5-flash；全生效配置检索 `pro` 仅命中 DeepSeek-V4-Pro（modelscope/ark，与 agnes 无关），agnes-2.5-pro 字样只存在于其他 Agent 留下的 `_chk*.json` 抓取缓存中。证据缺口需如实说明：AGNES 上游 /models 只返回 id/owned_by/supported_endpoint_types，**不提供任何价格字段**，故"flash 免费、pro 收费"无法由 API 自证，依据是厂商免费口径与本仓记录 + 用户 2026-08-29 确认 pro 收费。③Fast 编排扣费风险规避——结论=**四成员逐一下游实测全部 0 价**：openrouter/free 上游 pricing prompt=0 completion=0；商汤 sensenova-6.8-flash-lite prompt=0 completion=0；zenmux inclusionai/ling-3.0-tiny prompt=0 completion=0（该模型名无 -free 后缀但单价为 0，属命名误导不是收费风险）；改道用的 dots-studio/dots3-note-prev 亦 prompt=0 completion=0；渠道级 billing_tag 五家均为 🟢 免费/免费配额。收尾后再发一次真实 fast 调用复核：HTTP 200 / 1.0s，X-Routed-Channel=openrouter，resolved_model=openrouter/free，链中已无 xiaohongshu。④飞书 Base「模型白嫖渠道」表(tbl40PAWYub8G5m8)已按读-改-写同步 3 行（均返回 ok=True，原字段值从完整导出读取未截断）：recvtyc8maa4xc 小红书 网关落地状态 改为「已落地·暂不可用」并附实测数据与改道说明；recvtyc2l7ZsnP AGNES 追加 pro 三变体在架但未入选用/未进 fast 的计费核查结论；recvtyc7K9EilA ZenMux 追加 dots3 实测 0 价与 5.0~8.6s 延迟，并记录配置缺陷。⑤发现但**未擅自修改**的配置缺陷（按不扩范围原则仅上报）：`channel_models.json` 中 zenmux 选用项写作 `dots-studio/dots-3-note-prev`（多一个连字符），上游 166 个模型中不存在该名，正确名为 `dots-studio/dots3-note-prev`；实测直连 dots3-note-prev 时网关已能正确落到 zenmux，故当前未致错，但该条目为无效配置。⑥本条内嵌核验令牌（供上级模型实读本文件防编造，请勿改写）：P4FREE-AudIT-Xhs-Defer-2H8K-7M。（2026-08-28）
- **[S-20260827-02]** 各平台程序免费资源清单（2026-08-27 用户口述+AI查证）：①豆包工作(豆包电脑版)=下载/升级登录免费领30天订阅(8/25发布，≈一个月会员)；②AutoClaw(智谱效率工具)=GLM-5.3 Flash 免费体验；③Z Code(智谱编程工具)=新用户5天免费(GLM-5.3 3M tok/天+GLM-5-turbo 2M tok/天)，GLM Coding Plan 不定期7天免费体验卡；④WorkBuddy(腾讯，用户发音Work party)=混元Hy3限免至2026-08-31、0倍率不扣积分(Coding/Agent全开)，须8/31前用完；⑤Trae Work/TraeCode=智谱官方GLM-5.3抢先体验(TraeWork/扣子/WorkBuddy/Qoder/CatPaw/JoyCode/OpenCode均在名单)；背景=GLM-5.3(8/14发布,743B,开源)上线ZCode/AutoClaw+GLM Coding Plan全量。（2026-08-27）
- **[S-20260827-01]** 免费 AI 资源情报（2026-08-27 更新）：①empero 免费 API=free.empero.org/v1 提供 glm-5.3-flash+qwen3.8-flash-next，无密钥填Free、需浏览器UA；②Hermes Agent v0.20.5=免Key模型通道(opencode-free)可玩 Ox Alpha/HY3/Laguna S2.1/Nemotron 3 Ultra，7个免费模型无限用=ox alpha/longcat2.0/hy3/laguna-s/solar-pro4/step-3.7-flash；③OpenRouter 限免 GLM5.2=免费号每天50次；④8.AI 限免=DeepSeek V4 Flash/Vision+HY3+MiMo V2.5；⑤GLM Z Code/Coding Plan 活动免费额度；⑥AutoClaw 客户端自带 GLM5.3 Flash 免费体验；⑦ChatGPT Plus 性价比最高约4.6元/亿Token；⑧Ox Alpha 浏览量超5000得10天30美元额度。（2026-08-27）
- **[S-20260825-01]** AI视频平台免费额度实测闭环（2026-08-25，统一提示词一只橘猫在窗台上打盹，阳光洒进来，镜头缓慢推近，5秒/16:9）。9个平台实测结果：①生成成功3个=清影(200→400显示异常扣100无法确认，带水印)、Kling AI国际版(66→6扣60正确，带水印)、Pika(80→56扣24正确，无明显水印)；②已提交/排队并扣费3个=即梦AI(40→8扣32正确，Seedance2.0)、可灵AI(128→68扣60正确，视频3.0)、PixVerse中国版(100→62扣38正确，V6)；③免费额度不足1个=通义万相(每日签到仅10灵感值，单次720P/5s需15，无法生成，方案预期每日50未到账)；④套餐受限1个=Runway(125 credits充足但视频生成需Standard套餐)；⑤特殊1个=Vidu(Q3免费1次模型提交成功余额未变20，免费次数与积分体系并存)。海螺AI：new_user_credit API返回300但账户isNewUser=false非新用户，无领取入口不可领。飞书多维表格《平台积分领取测试结果》已回填13个平台(生成成功/扣减正确/总体结论/备注四列，base_token=StmDbTXQWaujshs9NpIc3UFpnAc)。（2026-08-25）
- 无。

## 卡点
- 无。

## 下一步
- 无。
