# STATE.md — 工具链/DSH 开发 项目状态

## 进行中
- 无。

## 已完成（最近）
- **[S-20260827-13]** 阶段3 验收窗口已执行并完整回退，结论：路由与协议转换全通，卡在 CC Switch 自定义 provider 的凭证覆盖，需上层裁定接入方式。①按 GPT 裁定走 B（临时 takeover 验收事务），基线冻结完成：live settings 基线 sha=d2fcd8dcd0273a49、原出口 :23333、原 current provider=80d9c9d3(API网关 V4 Flash)、sensetime/zenmux/opencode/modelscope 四渠道 rate-limit 台账 limit_rpm 均为 None（即 POLICIES 是 rate_limit.py:45 的硬编码字典，仅 xiaohongshu 60/min 与 openrouter 20min+50day 有静态限额，:3100 无 /api/rate-limits 写端点，故"GPT 说的临时调低 RPM"没有配置入口，只能临时加 POLICIES 条目并重启，等价且不碰 capability 表）。②已实证的正面结果：CC Switch 切到 gateway-3100 后，自身转发日志明确出现 [Claude] >>> 请求目标: http://127.0.0.1:3100/v1/chat/completions (model=deepseek-v4-...)，即 Anthropic Messages → OpenAI Chat 的路径改写与裸统一模型名传递全部正确，阶段3 的协议层判断成立。③阻塞点：:3100 始终回 401（响应体为网关自己的"未授权：此网关已启用 API key"，证明请求确实打到 :3100 而非 proxy 拒绝）。根因是 CC Switch 会用 live settings 里的旧 token 反向覆盖我写入 provider 的网关 key——我先后四次把 key 写进 providers.settings_config.env.ANTHROPIC_AUTH_TOKEN（网关 key 长度 10），每次重启后该字段都变回 42 位（与旧 provider「API网关 V4 Flash」完全一致），且 live 也被同步改写；已排除其他存储位置：providers 表无 token 列、settings 表无凭证项、无 Windows 凭据管理器条目、provider_endpoints 无该 provider 记录、meta 仅含 commonConfigEnabled/endpointAutoSelect/apiFormat，故凭证唯一落点就是 settings_config.env，但被双向同步逻辑持续覆盖。④本轮新增可复用的 CC Switch 实测事实（已同步进 global/TOOLS.md §6.7，另有两条被本轮修正）：a) 权威 current provider 指针是 ~/.cc-switch/settings.json 的 currentProviderClaude 字段，只改 providers.is_current=1 不生效——实测改完 is_current 后 proxy 仍转发 23333，改该字段才真正切换；b) proxy 忽略客户端传入的模型名，一律换成 current provider 自己的模型串，因此不存在按请求选 provider 的隔离能力；c) 回退锚点可靠：proxy_live_backup.original_config 用 json.dumps(...,ensure_ascii=False,indent=2) 重新序列化后哈希与基线精确一致，可作为字节级还原依据。⑤回退已按 GPT 第 13 步无条件执行并验证：live 还原后 sha=d2fcd8dcd0273a49 与基线一致、BASE_URL 回到 127.0.0.1:23333、DB 与 settings.json 双处 current provider 均恢复 80d9c9d3、proxy_enabled/enabled/live_takeover_active 全部归 0、15721 确认不再监听；回退后 python tests/test_search_gateway_routing.py 仍 35/35 退出码 0，:3100 进程健康，未对网关做任何代码改动。⑥重要边界声明：本条只算阶段3 验收 FAIL（未达 GPT 判据 1 的端到端成功请求），不宣布也不隐含 Cherry 退役、gateway-3100 保留在库中（is_current=0）等下次凭证问题解决后复用。⑦并发提醒：另一 Agent 正在同一 ai-hub 工作区修改 services/search_gateway/channels.py 的 channel_health（把 health_fallback_ok 也应用到 HTTPError 分支，解决 Cloudflare 对 /models 返 405/503 导致渠道被误判 unreachable，正是我上一轮报的 empero HTTP 503 那条线），该未提交改动不属本会话，切勿覆盖或回退。⑧待上层裁定：CC Switch 自定义 openai_chat provider 的上游 key 到底该怎么正确注入（走 GUI 粘贴一次是否为唯一可靠路径 / 是否有我未发现的同步抑制开关 / 或改判阶段3 验收改用其他 Anthropic 兼容层），以及是否接受"验收需人工在 GUI 做一次凭证粘贴"作为标准前置步骤。⑨本条内嵌核验令牌（供上级模型实读本文件防编造，请勿改写）：P3BLOCK-CCSWITCH-KEY-OVERRIDE-4T7W-ZQ。（2026-08-27）
- **[S-20260827-12]** 阶段3 CC Switch 并行接入执行结果 + 一条必须纠偏的规范冲突。①安全前置：手动备份 cc-switch.db 至 backups/db_backup_manual_20260827_222244.db，记录 ~/.claude/settings.json sha256=d2fcd8dcd0273a49 作回退基线，确认 journal_mode=delete 且无 wal/shm 残留。②写入方式=停 CC Switch(原 PID 22784)→直写 sqlite→重启（CC Switch 无控制 API，provider 只存 DB）：新增 claude provider name=gateway-3100 id=6fac8855-b076-4a01-a8d4-7017fc0ad5a7；meta.apiFormat=openai_chat（该枚举值实证来自同库 codex provider，现存 6 个 claude provider 全部是 anthropic）；settings_config.env 按用户拍板使用裸统一模型名 deepseek-v4-flash（不写渠道前缀，因 :3100 的 /v1/models 不认 <渠道>:<模型> 语法）；is_current 保持 0 未翻转，现用 provider 仍是「API网关 V4 Flash」。proxy_config 的 proxy_enabled 与 enabled 由 0→1，live_takeover_active 未触碰仍为 0。③重启后验证：127.0.0.1:15721 正常监听（新 PID 24108）；POST /v1/messages 实测 HTTP 200 / 3.6s / stop_reason=max_tokens，证明 Anthropic↔OpenAI Chat 双向转换可用；当前实际链路为 Claude Code→15721→现用 provider→Cherry 23333→上游，Cherry 仍在环内，:3100 尚未成为默认出口。④【关键纠偏】原以为「开本地 proxy」与「不改 Claude 全局配置」可并行，实测在 CC Switch v3.14.1 中二者不互斥：proxy 一旦 enable 即自动 takeover，~/.claude/settings.json 被改写（sha→fb2faf7c6058e8dc），ANTHROPIC_BASE_URL 由 http://127.0.0.1:23333 变为 http://127.0.0.1:15721，且 ANTHROPIC_*_MODEL 全被重写为 claude-opus-4-8 / claude-sonnet-4-6 / claude-haiku-4-5 / claude-fable-5 官方别名，原 <渠道>:<模型> 前缀名被移到对应 *_MODEL_NAME 字段。据此纠正上一轮据 live_takeover_active=0 推断「未接管」的错误结论——该标志并不控制此行为。此结果超出用户批准的「不动现有链路」范围，已如实上报并请更上级模型裁定。⑤回退路径已确认可用：CC Switch 自身 proxy_live_backup 表已存改动前原配置（app=claude、BASE_URL=23333、backed_up_at=2026-08-28T05:26:39Z）。⑥附带发现：GET /v1/models 模型数十分钟内 158→123 波动，列表随渠道健康缓存变化（探测失败即消失），对客户端表现为模型随机消失，建议单独记 backlog 不在本阶段处理；另 pre-flight 验证 deepseek-v4-flash 带 tools 时链上 5 候选中 modelscope 被 capability_mismatch 正确跳过、sensetime/zenmux/opencode 三家 eligible，说明该裸名可安全作为 Claude Code 出口。⑦待裁定选项：A 立即回退 takeover 恢复 :23333 直连，改走非接管式验证路径；B 保持接管态把阶段3 验收做完（切到 gateway-3100、临时调低首选渠道 rpm 注入失败、跑真实 Read/Bash 证明工具链不断链），再统一决定是否让 Cherry 退役。⑧本条内嵌核验令牌（供上级模型实读本文件防编造，请勿改写）：P3TAKEOVER-VERIFY-7Q4X-MB2Z-K9LOCKED。（2026-08-27）
- **[S-20260827-10]** :3100 冷启动上线 + P1.5 真实进程内验证 + CC Switch 接入前勘查。①交接说明的「PID 32912 跑 Phase 0 旧代码」已失效：该 PID 不存在、3100 端口无任何监听（旧进程 _boot_err.log 最后一条访问日志停在 8/26 22:05），性质由「重启」变「冷启动」，无在线客户端可被打断。确认无残留 api_gateway.py 进程后单进程启动，现 PID 33544 绑 0.0.0.0:3100，端口取环境变量 API_GATEWAY_PORT（默认 3100）。②启动后测试基线 python tests/test_search_gateway_routing.py = 35/35 通过、退出码 0。③验收项：无 Bearer GET /v1/models 回 401、带 Bearer 回 200 且 158 个模型；带 tools 的 POST /api/route-plan 得到 4 条 eligible:false 且 reason=capability_mismatch（channel=modelscope/upstream=deepseek-ai/DeepSeek-V4-Flash-0731 命中三个别名 deepseek-v4-flash、deepseek-v4-flash、deepseek-ai/DeepSeek-V4-Flash-0731；channel=xiaohongshu/upstream=dots3-note-prev），这 4 条均 reachable=true 且 key_set=true，说明 capability 判定是真生效而非被前置原因掩盖 → P1.5 在真实运行进程中确认上线。④empero 未按交接预期显示 capability_mismatch，而是 reason=unreachable：/api/channels 显示 empero enabled=true key_set=true reachable=false err=HTTP 503（上游侧 503）；而 reason 优先级 disabled>no_key>unreachable>capability_mismatch 是 P1.5 设计文档 §3 明确规定的，故属设计预期、非缺陷，其 capability_mismatch.unsupported=['tools'] 仍被正确算出仅未作主展示原因。empero 503 健康问题是另案。⑤新发现坑（重要）：build_route_plan 的 docstring 声称「只读、不触发上游请求」，但内部 cached_health_all(ttl=60) 会对过期渠道并发发起真实健康探测，即 /api/route-plan 并非纯只读、会把网络探测延迟计入响应；做 CC Switch 链路延迟评估时必须计入此项。⑥CC Switch 勘查三发现：Claude Code 的 ANTHROPIC_BASE_URL=http://127.0.0.1:23333 而 23333 属 Cherry Studio.exe，故 :3100 上线对现有 Claude 链路零影响；CC Switch 进程在跑但本地 proxy 全关（cc-switch.db proxy_config: proxy_enabled=0、enabled=0、auto_failover_enabled=0、live_takeover_active=0，updated_at 2026-07-01，监听位已配 127.0.0.1:15721，熔断与超时参数齐备），且与 settings.json 的 enableLocalProxy=true 相互矛盾，实际生效以 DB 的 0 为准；providers 表里名为「API网关 V4 Flash」「API网关 高级」的两个 claude provider 的 base_url 其实也是 127.0.0.1:23333 而非 :3100，名不副实、改造时易误判为已接入。⑦因 :3100 只有 /v1/chat/completions、无 /v1/messages，协议转换必须在 CC Switch 侧完成，故目标链路定为 Claude Code → CC Switch:15721 → :3100，GPT 建议的「不给 :3100 自造 /v1/messages」记入 backlog。⑧用户 2026-08-27 拍板三项：改动范围=并行接入不动现有链路（新建 gateway-3100 provider + 开 15721 proxy，不开 live takeover、不改现有 provider，可一键切回 Cherry）；429 故障注入方式=临时调低首选渠道 rpm 限额（mtime 热重载，验完恢复），不用真实刷量、不加 mock 假渠道；本次结论立即写回记忆。下一步：按上述范围做 CC Switch 接入与工具链不断链验收。（2026-08-27）
- **[S-20260827-11]** AI-Hub :3100 网关全阶段路线转交（用户 2026-08-27 拍板）：原镜像 GPT Thinking·Extended 问诊制定的多阶段路线（阶段0-6，源文件 D:\游戏\AI-Hub网关重点方向GPT问诊_2026-08-27.md）**全部转交其他模型执行**，本会话 Agent 不再推进。进度总览：✅**阶段0 基线冻结**（S-20260827-04，tag gateway-baseline-20260827）；✅**阶段1 自动切换**=失败归一化 upstream_outcome + route-plan 可观测 + 流式 commit point + 8-case failover（S-20260827-05/07，commit 9b7e478/961797a，GPT Extended 两轮评审 PASS）；✅**阶段1.1 不变量固化** 24 用例并入 run_all（8cbbbe0）；✅**阶段2 能力感知**=tools/vision/json_schema capability filter（S-20260827-08，PR#2 fe32d8f/c6422f2/bbceb8c，GPT 验读 PASS，令牌 CAP5-P15-OK-V3R1F1ED-2026）。⏳**未开始**：**阶段3** CC Switch 接 Claude Code 去 Cherry（Claude provider + API Format=OpenAI Chat Completions + upstream=127.0.0.1:3100 + 开 local proxy takeover，model=统一模型名；验收=关闭 Cherry 后 Claude 正常聊天+Bash/Read 工具正常+人为打坏首渠道仍自动 fallback 且 tools 正常；稳定后不再自写 /v1/messages adapter）；**阶段4** 资源控制平面（飞书→ai-resource-hub→validate→gateway_resources.json→:3100 mtime 热加载；绝不经飞书直连；credential_ref 进配置、key 绝不进 git）；**阶段5** 资源接入流水线（统一 discover→verify→benchmark→declare capabilities/quota→add channel→add unified member→failover test→active，不再为每免费模型单独立项）；**阶段6** webbridge 独立进程隔离（最后做，绝不让网页自动化逻辑进 :3100 核心）。接手第一动作：向用户确认重启 :3100（Get-CimInstance 杀光 api_gateway.py 再起单进程，上线 Phase1+P1.5 新代码）→ POST /api/route-plan 带 tools 的 payload 验证 capability 过滤真实生效 → 按 GPT 排序推进阶段3。强制纪律（沿用）：每阶段完成必须镜像 GPT Thinking·Extended 评审（单任务≤3 轮内收敛）、浏览器 tab new 不抢占已有标签、窗口≤12 轮。（2026-08-27）
- **[S-20260827-09]** P1.5 GPT 验读 PASS 确认 + 任务转交（2026-08-27）。①P1.5 汇报的 GPT 回复已取回：vip-12 会话「⑫ 验读通过设计路由」（/c/6a90ee8f），9935 字符，开头「验读 PASS」，逐项 a) 令牌 b) tool_choice 断言 c) 禁名字猜测注释 d) route_completion 内 capability_mismatch 位于 key_is_set 前 e) build_route_plan(model,payload=None) f) test_capability_routing 组+行数——全部 PASS，宣布 PR #2 Done，并重申下一阶段排序（capability 落地后→CC Switch/Claude Code tools 实战验收 与 route observability）。回复全文存档 D:\游戏\AI-Hub_P15验读回复_GPT_2026-08-27.md。②本任务按用户指示转交其他 AI 接手，转交语已交付用户（自包含 Markdown）。③接手 Agent 第一动作建议：向用户确认是否重启 :3100（杀旧 api_gateway.py 进程起单进程，上线 Phase1+P1.5 新代码），重启后用 POST /api/route-plan 传带 tools 的 payload 验证 capability 过滤真实生效；随后按 GPT 排序推进 CC Switch/Claude Code 直连实战或 route observability。交接包：reports/AI-Hub_阶段成果包_Phase1封板_P1.1_P1.5.html + 本 STATE 条目 S-20260827-01..09 完整时间线。（2026-08-27）（2026-08-27）
- **[S-20260827-08]** P1.5 capability-aware routing 完成并推送（2026-08-27，GPT Extended PR#2 设计落地）。交付：①capabilities.py 新增（六能力常量/required_capabilities 推导含 tool_choice 与 content-array image_url/tri-state model_capabilities 三级解析/capability_mismatch 仅 false 进 mismatch/check_candidate 一次返回；mtime-aware 三级回落 MODEL_CAPABILITIES_FILE>data/运行时>随仓 default）；②model_capabilities.json+.default.json（empero glm-5.3-flash、xiaohongshu dots3-note-prev 登记 tools=false vision=false，modelscope DeepSeek-V4-Flash-0731 tools=false vision=false json_object=true；false=本地skip、未知=放行）；③api_gateway.py 接入（route_completion 循环内 key 检查前 capability admission，mismatch 记 failures 含 capability_mismatch 子对象且保留 {channel,outcome,detail} contract，零上游访问/零 key/零 try_acquire/零 breaker；build_route_plan(model,payload=None) 扩展，reason 优先级 disabled>no_key>unreachable>capability_mismatch>blocked>quota，顶层 required_capabilities；do_GET ?payload= 与 do_POST /api/route-plan）；④测试：test_capability_routing 11 用例，总 35/35 连续3次，0真实API，0secret，P1.1 24用例无回归。commit：fe32d8f/c6422f2/bbceb8c，P15 验收令牌 CAP5-P15-OK-V3R1F1ED-2026。GPT 验读：P1.5 汇报已发镜像站（vip-11 3连空回复后切 vip-12，Extended 已确认），回复因浏览器扩展桥断未取回——事实核验已用 GitHub API+本地测试双源完成（HEAD=bbceb8c 公开可见）。同日新增规则：共享浏览器标签页纪律（不抢占已有标签，tab new 新开）写入 global/TOOLS.md（84e9fce）；GPT 问诊窗口轮次上限 42→12、单任务≤3轮（D-GLOBAL-20260827-01，70d7c6b/b9d8656）。遗留：:3100 运行进程仍 Phase 0 旧代码待重启（Phase1+P1.5 新代码待上线）；浏览器 opencli 扩展桥断开待重连。（2026-08-27）（2026-08-27）
- **[S-20260827-07]** GPT Thinking·Extended 评审完成，Phase 1 验收 PASS（2026-08-27）。过程：镜像站 vip-11 复用「评审限流切换方案」历史会话，Thinking·Extended 模式两轮（第1轮设计评审 PASS+边界问题清单；第2轮内嵌 upstream_outcome/build_route_plan 关键代码证据消除公开仓库缓存疑虑后最终验收）。最终结论：①Phase 1 四项交付（失败归一化/route-plan 可观测/流式 commit point/8-case 回归）正式 PASS，P1 闭环成立，停止打磨。②第1轮曾因 GitHub 页面 CDN 缓存误判「公开仓库无 upstream_outcome.py」，实测 raw 直拉 9b7e478 已公开，疑虑消除。③GPT 高风险修复建议（优先级最高）：upstream_outcome._classify_text() 关键词过宽——QUOTA 的 limit、AUTH 的 key/token 属低置信度裸关键词，context length limit exceeded/max token limit 等请求级错误会被误判为 QUOTA/AUTH 并错误熔断健康渠道；建议分三层改（高置信 phrase→显式排除 context length/max token→删除裸词），补 6 个误判测试。④下一步排序（GPT 拍板）：P1.1 收尾 PR = 8-case+边界固化成 pytest CI（test_upstream_outcome/test_rate_limit 含 10 线程并发 try_acquire 仅 1 成功/test_failover 全 mock 含失败聚合 contract 断言/test_stream_commit 含 commit 后断流 B 调用数==0），验收=连续 10 次一致+零真实 API 调用；随后 PR #2 = capability-aware routing（tools/vision/json_schema，capabilities.py + payload 推导 required + route-plan 扩 capability_mismatch，验收=A 不支持 tools 时零上游调用且 B 顶上用户模型名不变）。⑤commit 9b7e478 已推送 refactor/monorepo-20260812（交接待办①完成）。评审全文存档 D:\游戏\AI-Hub网关第1阶段GPT评审_2026-08-27.md。遗留：:3100 运行进程仍为 Phase 0 旧代码，重启时先杀光 api_gateway.py 旧进程再起单个。（2026-08-27）（2026-08-27）
- **[S-20260827-06]** :3100 网关 Phase 1 交接（2026-08-27，GPT 评审未完成即转交）：已完成=①upstream_outcome.py 失败归一化（Outcome 枚举+classify_http_status/classify_shell/is_breaker）②route_completion 接入+失败聚合 ③/api/route-plan 可观测接口 ④流式 commit point 不变量 ⑤8-case failover 回归 ALL PASS（脚本 D:\游戏\_隔离\phase1_failover_test.py）⑥文档已更新 TOOLS.md§6.6+STATE S-20260827-05。待办=①D:\项目 ai-hub 工作区未提交（api_gateway.py 改+upstream_outcome.py 新增，branch refactor/monorepo-20260812）②GPT 镜像 Extended 评审未完成：镜像会话 n8hh7hyn 被小红书任务反复切走+9 分钟无回复，提示词 D:\游戏\_隔离\gpt_phase1_prompt.md，旧 Auto 回复在 D:\游戏\AI-Hub网关第1阶段GPT评审_2026-08-27.md（无效需 Extended 重发）③网关运行进程 PID32912 仍为 Phase 0 旧代码，重启前先杀旧进程。用户要求：每阶段完成必须用 Thinking·Extended 模式找镜像 GPT 评审。（2026-08-27）
- **[S-20260827-05]** :3100 网关 Phase 1 完成（2026-08-27，GPT 问诊 P0/P1 落地，基于 gateway-baseline-20260827）：
  - 失败归一化：新增 `upstream_outcome.py`，上游错误统一为 Outcome 枚举（SUCCESS/RATE_LIMIT/QUOTA/AUTH/MODEL_UNAVAILABLE/OVERLOADED/PROTOCOL_ERROR/TIMEOUT）；`classify_http_status` 映射 HTTP 状态码（429→RATE_LIMIT、503→OVERLOADED 等）、`classify_shell` 检测 200+错误载荷空壳、`is_breaker` 判定熔断类型（RATE_LIMIT/QUOTA/AUTH/OVERLOADED 触发指数退避）
  - 接入 route_completion：shell 合成 429 升级为归一化 failure type + failures 聚合（failure 带 channel/outcome/detail，替代原只有 channel 的失败列表）
  - 路由可观测：新增 `GET /api/route-plan?model=`，返回候选渠道链 + 每渠道 eligible/reason/used_1m/limit_1m/state/blocked_in，解决"为何未走预期渠道"排障
  - 流式 commit point 不变量：failover 仅限 response commit 前（首包 _peek_stream 通过即 commit，此后不得换上游），避免多模型拼接
  - 回归验证：8-case failover 矩阵 ALL PASS（正常/HTTP429/200+quota shell/timeout/503/blocked 本地 skip/blocked 恢复/10 并发 95% 穿透仅放行 1）——测试脚本 `D:\游戏\_隔离\phase1_failover_test.py`
  - 未改动：unified_models / routing / rate_limit 既有模块（复用 try_acquire/record_result）
  - 遗留（Phase 1 范围外）：capability-aware routing（tools/MCP 断链，P1.5）、CC Switch 接 Claude Code（P2）、飞书资源物化（P3）
- **[S-20260827-04]** :3100 网关基线冻结（Phase 0，2026-08-27，GPT 第0阶段方案执行）：
  - canonical branch / SHA = refactor/monorepo-20260812 @ 8ec04de1f1e0a09c68668858ea79f34b22bf741e（tag: gateway-baseline-20260827）
  - runtime 进程 = PID 32912 → D:\项目\services\search_gateway\api_gateway.py（与 canonical 一致，工作树干净）
  - 当前已实现：unified model routing（unified_models.json）/ 用户手工渠道顺序（routing.json）/ reachable+health cache 60s / rate_limit.try_acquire 容量准入 / 95/85 hysteresis / 429 breaker（Retry-After 优先 + 指数退避 15-300s）/ 200+quota shell failure failover / blocked 本地 skip
  - 已验证（failover 回归矩阵 4/4 PASS）：首渠道成功 / HTTP 429 failover / 200+quota shell failover / blocked 本地 skip
  - 原 S-20260827-03 "仅 reachable+健康缓存 fallback，缺容量预警+自动切换"描述已过时（容量准入+熔断+自动切换已实现）
  - main 分支决策：main 已分叉且不含 services/search_gateway（main 独有 1 提交 2d49b5e 未进 refactor；refactor 独有 51 提交），暂不合并 main，cutover blocker = monorepo merge scope
  - 下一阶段（Phase 1+，未做）：failure normalization / capability-aware routing / route observability / Retry-After 完整策略（2026-08-27）
- **[S-20260827-03]** 用户重点项目方向（2026-08-27 确认，优先级从高到低）：①API转发网关模型自动切换=一个模型名背后多模型流转、配额快耗尽自动切下一个（第一重点；当前仅 reachable+健康缓存60s fallback，缺容量预警+自动切换（后续状态已由 S-20260827-04 更新：容量准入+熔断+自动切换已实现））；②工序链断裂=切换模型后下一个模型不能用上一个用过的工具(MCP工具上下文)，需研究网关层/客户端层解决；③Claude Code 转发简化=不再走 Cherry Studio、直连自建网关 :3100，考虑 CC Switch 管理 provider；④飞书白嫖资源落实=免费 API 统一配置成渠道+路由规则；⑤新免费模型接入=GLM5.3 Flash(Z Code 活动)/千问3.8(27B/Flash/Flash-Next)/AutoClaw 的 GLM5.3 Flash 免费体验；⑥AI网站转 API=网页版 AI 封装成 OpenAI 兼容 API。（2026-08-27）
- **[S-20260827-02]** empero 免费渠道接入（2026-08-27）：free.empero.org/v1 无真实密钥填 Free、channel 定义必须带浏览器 UA 字段绕过 Cloudflare 1010；模型 glm-5.3-flash + qwen3.8-flash-next（Qwen4 架构预览 125B/6B 激活）。同时按用户指示停用 zhipu_coding（GLM Coding Plan 额度用完，enabled=0），glm-5.3-flash 统一改由 empero 提供（unified_models + routing 同步改）。Cherry Studio 已同步。坑：上游偶发 503 overloaded/模型进程重启；qwen3.8-flash（非next）临时退役让位 GLM；POST /api/channels 接口不支持 ua 字段，需直接改 channels.json custom_channels。待办：empero 速度验证、hy3 via opencode 403 code 1010 排查。（2026-08-27）
- **[S-20260827-01]** 斩杀线模型分层（2026-08-26~27，用户拍板以小红书 dots3-note-prev 为斩杀线）：斩杀线以下模型从主列表隐藏、归为 fast 快速补充——hidden=sensenova-6.8-flash-lite/agnes-2.5-flash/inclusionai-ling-3.0-tiny（model_overrides.json hidden 数组 + unified_models.json tier 标记 + api_page.html ⚡快速补充标签 + sync_cherry.py is_hidden 同步），但路由仍可调用。主力组新增 dots3-note-prev(小红书)、hy3(腾讯混元 via opencode)。验收：/api/models name 字段确认主力模型在列；Cherry Studio 已同步隐藏标记。（2026-08-27）
- **[S-20260825-07]** 方舟开通管理额度实查+Seedream5.0首图验证（2026-08-25，kimi-webbridge 操控用户真实 Chrome 读控制台表格）：已开通且有免费额度=Seedream-5.0-lite剩50张(模型id doubao-seedream-5-0-260128)、Seedream-4.5剩200张(4-5-251128)、4.0剩200张(4-0-250828/20260415)、Seed3D-2.0剩200万tokens、Hyper3D-Gen2剩15万、Hitem3D-2.0剩50万、Wan2.1-14B剩200万、各已开通LLM各50万；未开通但开通即得200万=Seedance-1.0-pro-fast/pro/lite-t2v/lite-i2v、1.5-pro、Seed-Evolving；Seedream-5.0-pro已开通但服务暂停且无免费额度(0.3-0.6元/张)避免使用；安心体验已开启(超额自动暂停)。实测：网关 /v1/images/generations → doubao-seedream-5-0-260128 → 2560x1440 出图23s，仙逆风格质量极佳(右下角AI生成小水印可裁)。控制台表格自动化要点：视觉模型tab下分2页、tr行读取、分页按钮数字匹配。下一步：Seedream批量二创官方王林形象(50+200+200张额度充足)、Seed3D图生3D管线（2026-08-25）
- **[S-20260825-06]** API网关新增火山方舟渠道+生图端点（2026-08-25，用户提供 ARK key 存 channels.json 信任平面）：①channels.py 新增 ark 渠道（base_url=ark.cn-beijing.volces.com/api/v3，models_path 动态拉取 + channel_health 新增 ark 分支过滤 Shutdown/Retiring，CHANNEL_ORDER 插 sensetime 后）；②api_gateway.py 新增 POST /v1/images/generations 生图转发：模型路由 seedream*/seededit*→ark、sensenova-u1*→sensetime，透传响应+X-Resolved-Channel 头+quota 记录；③验证：/api/channels ark reachable=true 40 个活跃模型（滤掉 Shutdown/Retiring 后），管道零成本测试（非法 size 触发上游 400 校验错误=全链路通）。ARK 模型菜单已呈用户选择：生图 seedream-5-0/5-0-pro/4-5/4-0、3D seed3d-2-0+hyper3d-gen2+hitem3d-2-0、视频 seedance-2-5/2-0、角色一致性 seed-character-260628、LLM deepseek-v4/glm-5.2/doubao-seed-2-1。坑：Seedream5.0 最小 3686400 像素（2560x1440 起），旧 2048x1152 不够；网关 /api/* 有 Bearer 鉴权（key 在 api_state.json）。分工确认：用户选模型防误扣费，快速模型跑浏览器/重复任务，主 Agent 做架构高质量任务。下一步：用户选定模型后接入逆天主题二创管线（2026-08-25）
- **[S-20260825-05]** DSH 选择器只显示网关统一编排模型（用户拍板：非 30 模型全清单，只要编排组，且要自动同步）。①web profile patch（~/.dsh/profiles/web/cordis.patch.yml）按 id 禁用 llm-deepseek 行 → 选择器不再混官方 DeepSeek 组（DeepSeek-V4-Flash/Pro）；pi-ai 内置目录本就不注册路由无需处理。②channels.py set_unified_model/delete_unified_model 挂 sync_dsh_models() 钩子：unified_models.json 组名→settings.yaml local-gateway.models 的 id、display→显示名 name；DSH settings 热重载免重启；默认模型被删自动回落第一项；全删空不清空 DSH 保留末份清单。③坑：网关解释器 workbuddy python 3.13.12 缺 PyYAML 导致钩子静默失败（try/except 吞掉），已装 pyyaml 6.0.3——换解释器/升级后必须重装。④顺带修复当日 DSH 整页白屏（Failed to load plugins: i is not defined）：nitian-dsh-theme lib/client.js subTier() 内残留死代码 if (i >= 15) { } 引用未定义 i；**插件运行时真身是安装副本 ~/.dsh/profiles/web/node_modules/<pkg>（真实拷贝非软链），改源码 ~/.dsh/nitian-stage 无效**；服务内存缓存 bundle 须重启 DeepSeekHarness 服务 + 浏览器 cache-buster 强刷；patch 文件改动则 watchUserPatches 热应用免重启。⑤主题 Agent 重写 patch 文件（插件行 id 改 nitian-dsh-theme-r2 触发重挂载）时抹掉了 llm-deepseek 禁用行，已补回并浏览器复验——多 Agent 同改此文件须保留彼此行。验收：选择器=local-gateway 单组 4 条（Fast 免费快线/deepseek-v4-flash/ox/glm-5.2），POST /api/unified 幂等 upsert 实测触发 settings.yaml 重写（mtime 变化）。遗留：headless profile 未禁 llm-deepseek（CLI 场景仍有官方组）；settings.yaml models 清单自此由网关编排接管，手动加非编排模型需建统一组否则被下次同步覆盖；:3100 前端视觉验收仍待郭老师目检。（2026-08-25）
- **[S-20260825-04]** :3100 转发光线芯片点击弹窗换上游模型（渠道已筛选置顶+点选未筛选自动加入渠道筛选，双向同步，纯前端零后端改动）；:3000 hub_page / :8791 canvas index+gallery / :8000 导航页+dashboard 全部注入多风格皮肤层（apple 默认保持现状/huawei/xiaomi/kawaii/xiuxian/midnight，悬浮🎨切换+localStorage 记忆，规范落 services/THEME_GUIDE.md）；:3100 启用 API key 鉴权（api_state.json），DSH settings.yaml 同步真实 key 修复 401；:8000 central server 提权重启。全部 node --check/接口级验证通过。（2026-08-25）
- **[S-20260825-01]** API 转发网关 :3100 渠道管理五连修 + zenmux 接入 + DSH 切本地网关（2026-08-25）。①统一编排新建保存失败修复（POST /api/unified 落 do_POST）；②cdVendor 加 GEMMUX；③channel_health 三失败分支改回 ch.get(models) 兜底（停用/无 key 渠道模型不再显示 0）；④渠道管理新增自定义渠道+隐藏渠道：channels.json 增 custom_channels/hidden_channels，_save_config 后必调 _merge_custom_into_globals() 把自定义塞回模块级 CHANNELS/CHANNEL_ORDER（免改 ~20 处调用点），ordered_channels()=CHANNEL_ORDER 减 hidden 用于全部展示/聚合循环；端点 POST /api/channels（含 proxy 字段）、PUT /api/channels/<cid>/hidden、DELETE /api/channels/<cid>（仅自定义可删）；前端 #/channels 加新增弹窗+已隐藏恢复区；⑤渠道列表美化：图标 46px 磁贴、名称 16px、计费徽章挪副行；⑥zenmux 渠道接入：Cherry Studio sqlite 读 provider（baseUrl https://zenmux.ai/api/v1 + key），全量 165 模型调查，模型=用户启用 4 个（deepseek-v4-flash-vision-exp-free/dots3-note-prev/ling-3.0-tiny/glm-4.7-flash-free）且策展 selected 同步设置；**zenmux 必须走本机代理 http://127.0.0.1:7890 直连超时**，POST 白名单因此补 proxy 字段；实测 ling-3.0-tiny 全链路通（z-ai 免费模型上游 429 属上游拥挤非网关问题）。⑦DSH settings.yaml 重写：唯一 provider=local-gateway(:3100/v1)，删 opencode-go/openrouter/modelscope/sensetime/agnes/zscc/xiaohongshu 七个直连，30 模型清单取自 GET /v1/models（备份 settings.yaml.bak-20260825），agent-default-model=local-gateway/deepseek-v4-flash。**运维坑：重启 :3100 必须 Get-CimInstance 杀光所有 api_gateway.py 进程再起单个**——旧 listener 未死会继续占端口跑旧代码，新端点 404 早加端点正常，极易误判代码未生效（本次 DELETE 曾中招）。前端视觉验收（#34 美化+新弹窗）待郭老师浏览器目检。（2026-08-25）
- **[S-20260815-05]** R18 checkpoint 全链路最终验证（S-20260815-10）（2026-08-15）
## 已完成（最近）
- **[S-20260815-04]** R18 Memory Checkpoint 落地（2026-08-15，GPT 评审定稿）：RULES.md 加 R18（事件优先+10 用户回合 watchdog），AGENTS.md/SKILL.md 同步引用，memory.py 新增 bootstrap（唯一入口注入规则+上下文）与 checkpoint（幂等保存+自动 commit/push）命令。（2026-08-15）
- **[S-20260815-03]** 换 Agent 无缝衔接的交接消息模板（用户 2026-08-15 定稿；完整流程见 ai-hub-memory/docs/Agent记忆上报指令.md 的「指令正文」）。交接时把下面整段发给当前 Agent：当前阶段已完成，请做好收尾交接：1) 读取 ai-hub-memory/docs/Agent记忆上报指令.md 的「指令正文」照着做，把本项目阶段成果整理成记忆写入共享记忆仓库(ai-hub-memory)；2) 关键动作：git pull --ff-only；判断归属（已注册项目 teaching/courseware/memory-system/english-teaching/english-learning/devel-tools 直接写，全新项目用 register，拿不准用 capture，清单以 MEMORY.json 为准）；python scripts/memory.py write --project 项目id --kind state --sid S-日期-序号 --content 交接内容(做了什么/验收/下一步/遗留)；python scripts/memory.py validate；git add -A && git commit -m memory-项目名-阶段交接 && git push；3) 回报项目 id + SID + 是否已 push。凭证绝不写入记忆；pre-commit 拦截按提示修复，禁 --no-verify。（2026-08-15）
- **[S-20260815-02]** GitHub 本地只读镜像备份设施上线：本机 _github-mirror 下以 git mirror（裸仓库）镜像全部 5 个 GitHub 仓库（ai-hub-memory/ai-resource-hub/ai-hub/feishu-data-hub 公开可匿名拉取 + english-teaching-production 私有）。方向严格 GitHub→本地，mirror 只 fetch 不 push、本地不可改动（mirror 裸仓库机制保证）。每日 22:00 由 Windows 计划任务 dsh-github-mirror-daily 自动 fetch --prune 同步；同步脚本 sync_mirrors.ps1 与日志 _sync.log 同目录，零凭证落盘。私有仓 english-teaching-production 已实测：本机 SYSTEM 上下文 git 可匿名验证/经本机会话凭据成功 fetch，daily 增量更新可用（无需额外配置）；仅当其此前未被本机凭据覆盖时才需用户侧一次授权。（2026-08-15）
- **[S-20260815-01]** DSH 模型 429 重试默认值调优：DEFAULT_MAX_RETRIES 从 2 改为 20（deepseek-harness packages/llm/llm/src/retry-policy.ts）。这是所有提供方省略 retryPolicy 时的共用默认预算（EMPTY_RESPONSE/RATE_LIMIT/SERVER/TIMEOUT/TRANSPORT），429(RATE_LIMIT) 因此由 2 次记为 20 次；显式配置不受影响。同步更新双语 README + 补丁 5 处测试断言默认值，449 单测通过、build:lib 产物含 =20。需重启 DSH Web 进程后生效（运行中进程仍用旧默认）。（2026-08-15）
- 无。

## 卡点
- 无。

## 下一步
- 无。
