# GPT Extended 问诊：:3100 端口收窄 R2 裁定实施评审验收（2026-08-29）

## 一、核验令牌（回复首行必须原文引用，证明实读本文档）
GW-BIND-R2IMP-2026-0829-847-VERIFIED

## 二、背景
- 上一轮你裁定 R2-GW-BIND-NARROW-2026-0829（存档 `gpt56_端口收窄安全加固裁定_2026-08-29.txt`）：运行态 PASS，设计闭环 A-，需补「空值/通配绑定保护」升 A。你列出的最高优先级三项为：
  - **P1**：修复 `API_GATEWAY_BIND=""` 的 wildcard fail-open
  - **P1**：补 localhost 实际消费者 smoke test
  - **P2**：把「192.168. 零引用」升级为「所有 3100 引用分类后非 loopback caller = 0」
- 本轮已完成 P1 两项并附送服务化配套核验，请求你做实施评审验收（A 级闭环判定）。

## 三、已验证事实（均可机器复核，代码在 refactor/monorepo-20260812 分支工作区，尚未提交）

### 1. P1-A：空值 fail-open 修复 + wildcard 显式授权门（api_gateway.py）
```python
BIND_RAW = (os.environ.get("API_GATEWAY_BIND") or "").strip()
BIND_HOST = BIND_RAW or "127.0.0.1"
BIND_WILDCARD_UNAUTHORIZED = (BIND_RAW in ("0.0.0.0", "::")) and os.environ.get("API_GATEWAY_ALLOW_WILDCARD") != "1"
# ... 启动前：
if BIND_WILDCARD_UNAUTHORIZED:
    print("[FAIL-CLOSED] API_GATEWAY_BIND=%r 请求全接口监听但未显式授权：需设 API_GATEWAY_ALLOW_WILDCARD=1 才允许。拒绝启动。" % BIND_RAW, flush=True)
    sys.exit(CONFIG_ERROR_EXIT)  # 3104
```
- 空串/纯空白 → `strip()` 后为空 → 回退 `127.0.0.1`（杜绝 Python 将 `''` 解释为 INADDR_ANY 意外 wildcard）
- `0.0.0.0`/`::` → 仅当 `API_GATEWAY_ALLOW_WILDCARD=1` 显式授权才允许；否则启动即退（fail closed，退出码 3104）

### 2. P1-A 配套：NSSM 恢复策略（已配置并核验）
- `nssm get API3100Gateway AppExit`：**Default=Restart；3102/3103/3104=Exit**（普通崩溃自动重启，配置错误/端口占用/bind 失败不 crash-loop）

### 3. P1-A 单测：test_gateway_bind.py 8/8 PASS（真实子进程探测 BIND_HOST/WILD 标志）
- 未设置→127.0.0.1/False；空串→127.0.0.1/False；纯空白→127.0.0.1/False；0.0.0.0 未授权→拒绝标志；:: 未授权→拒绝标志；0.0.0.0+显式授权→允许；具体 LAN IP→正常解析；localhost→正常解析

### 4. P1-B：localhost 真实消费者 smoke test PASS
- `C:\Users\郭永涛\.tools\smoke_localhost.py`：读 `api_state.json` 的 api_key，POST `http://localhost:3100/v1/chat/completions`（model=deepseek-v4-flash）→ **status=200**，usage 正常（completion_tokens 9 / prompt_tokens 5）
- 证明「配置用 localhost:3100 的客户端」真实可调（非仅 curl 127.0.0.1）

### 5. 服务化配套核验（R2 验收矩阵补充项）
- 服务 `API3100Gateway`：**Running / Automatic / NT SERVICE\API3100Gateway**
- TCP LISTEN 3100 恰 **1** 个，IPv4 唯一地址 **127.0.0.1**（0.0.0.0:3100=0，[::]:3100=0）
- listener PID **39208**（python.exe）← 父进程 38508（nssm.exe）→ **属于 API3100Gateway 服务进程树** ✓
- 无 key `/v1/models`=401（鉴权正常）；本机 `/healthz`=200（此前核验）

## 四、待评审问题
1. P1 两项（空值 fail-open 修复 + localhost smoke）是否已正确闭环？可否据此把 R2 设计闭环评级从 A- 升为 A？
2. wildcard 显式授权门（`API_GATEWAY_ALLOW_WILDCARD=1`）+ 3104 退出码 + NSSM「3102/3103/3104=Exit、Default=Restart」组合是否是最稳妥形态？有无遗漏边角？
3. 剩余 **P2**（泛化 3100 引用扫描：全量枚举分类后非 loopback caller=0，含 10/8/172 网段、主机名/.local/mDNS、.env/XML/注册表/NSSM 环境/计划任务、WSL/Docker/Hyper-V 确认；+ Windows 防火墙 3100/Python 过宽入站 Allow 检查）与「服务 restart 后仍只监听 127.0.0.1」复验，是否可作为下一轮收尾（不阻塞本轮 A 级判定）？
4. 当前实施（未提交状态）是否还需要补任何机器可验证的判据才能算「安全加固闭环完成」？

## 五、纪律自查
- 凭证：本问题包不含任何 key/token/JWT 值；仅引用本地脚本路径与代码片段。
- 轮次：本任务第 3 轮（R1=服务化常驻，R2=端口收窄，本轮=R2 实施评审）。后续追问请引用本轮裁定编号。
- 实读要求：请打开 `https://raw.githubusercontent.com/201650545/ai-hub-memory/master/docs/ai-advice/gpt56_端口收窄R2实施评审问诊_2026-08-29.md` 核验令牌后作答。
