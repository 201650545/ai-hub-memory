# GPT Extended 问诊：:3100 API 转发网关端口收窄安全加固评审（2026-08-29）

## 一、核验令牌（回复首行必须原文引用，证明实读本文档）
GW-BIND-NARROW-2026-0829-127-VERIFIED

## 二、背景
- :3100 = 多厂商 LLM API 聚合转发网关（OpenAI 兼容 /v1/chat/completions），入口 `D:\项目\services\search_gateway\api_gateway.py`（纯 stdlib，`ThreadedServer(("0.0.0.0", 3100))`）。
- 2026-08-29 已按你上一轮裁定（R1，存档 `gpt56_网关常驻方案裁定_2026-08-29.txt`）完成服务化常驻：NSSM 包装为 Windows 服务 `API3100Gateway`（NT SERVICE 虚拟账户 + Delayed Auto Start + fail closed + /healthz）。其中 R1 有一条条件性建议：「若仅本机调用可将 `0.0.0.0:3100` 收为 `127.0.0.1:3100`（需先确认无 LAN/Cherry 同步调用依赖）」。
- 本次已确认该条件成立并完成收窄，请求你做评审验收。

## 三、已验证事实（均可机器复核）
1. **全部客户端走本机地址，无局域网依赖**：
   - `D:\英语教学\00_工具\engine\text_searcher.py`：`DEFAULT_GATEWAY_URL = "http://127.0.0.1:3100"`
   - `D:\项目\services\search_gateway\content_pool.py`：`"http://127.0.0.1:3100/v1/chat/completions"`
   - `D:\项目\config\gateways.json` 与 `config\runtime.yaml`：`url: http://localhost:3100`
   - Cherry Studio 活跃配置（`AppData\Roaming\CherryStudio` 非 logs/telemetry）无 3100 引用；CC Switch 本地 proxy 默认 `listen_address='127.0.0.1'`，其 baseUrl 均为外部厂商（openrouter/zscc/google）
   - 全网 `192.168.x.x:3100` 引用计数 = 0（含 D:\项目、ai-resource-hub、.tools 的 py/ps1/bat/json/yaml/toml/ini）
2. **代码变更**（commit `25935db` @ refactor/monorepo-20260812，`api_gateway.py` +4/-2）：
   - 新增 `BIND_HOST = os.environ.get("API_GATEWAY_BIND", "127.0.0.1")`（与现有 `API_GATEWAY_PORT` 环境变量覆盖模式一致，默认仅本机，可覆盖回 0.0.0.0）
   - `_bind_server` 改用 `ThreadedServer((BIND_HOST, port), handler)`；启动打印同步 `http://BIND_HOST:PORT`
3. **验收实测（2026-08-29）**：
   - 服务 `API3100Gateway` Running；监听器 `127.0.0.1:3100`（此前 0.0.0.0），listener 恰 1
   - 本机 `/healthz` → `200 {"ok": true}`
   - 局域网 `192.168.1.134:3100` → 连接被拒（收窄生效）
   - `/v1/models` → 401（API key 鉴权正常，接口可达）
   - 路由回归测试 `test_search_gateway_routing.py`：**35/35 PASS**（零影响现有路由）
4. 文档同步：`global/TOOLS.md` §6.6 已补充绑定地址说明；任务转交文档第十一节记录完成。

## 四、待评审问题
1. 收窄到 `127.0.0.1` 是否正确、还有无遗漏的局域网调用方？给出需补查的清单（如有）。
2. `API_GATEWAY_BIND` 环境变量覆盖设计（默认 127.0.0.1、可回退 0.0.0.0）是否合理？有无更稳妥的形态？
3. 收窄后「局域网无法访问」是否构成对未来接入（如手机/平板访问网关 Web UI、Cherry 走 LAN）的合理取舍？若将来需要，回退成本如何？
4. 验收判据是否完整？缺什么机器可验证的项（参照否定性判据需 scan/count 的纪律）？

## 五、纪律自查
- 凭证：本问题包不含任何 key/token/JWT 值，只引用客户端地址与代码片段。
- 轮次：本任务第 1 轮（后续轮次追问请引用本轮裁定编号）。
- 实读要求：请先打开 `https://raw.githubusercontent.com/201650545/ai-hub-memory/master/docs/ai-advice/gpt56_端口收窄安全加固问诊_2026-08-29.md` 核验令牌后作答。
