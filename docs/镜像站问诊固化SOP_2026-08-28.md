# 镜像站问诊固化 SOP（2026-08-28 · 最快路径版）

> 目标：任何 Agent 拿到本文档即可用**最少步骤、最短时间**完成一次 GPT 镜像站问诊。
> 前置：opencli 可用；共享会话 n8hh7hyn 属于其他 Agent，**默认用独立会话 gptreview**（vip-12 已登录）。

## 标准流程（5 步，正常耗时 ~4 分钟）

| 步 | 动作 | 命令/要点 | 预期耗时 | 通过判据 |
|---|---|---|---|---|
| 1 | 确认会话在线 | node "D:\opencli-app\dist\src\main.js" browser gptreview state → URL 非 about:blank 即在线 | 5s | URL=vip-12.67673.live |
| 1a | （仅离线时）拉起 | browser gptreview open "https://vip-12.67673.live/" + sleep 10 | 15s | editor:true |
| 2 | 切 Extended | eval 点击 Auto pill 的 svg（pointer/mouse 五连完整事件序列）→ 等 3s → 点 [data-testid=model-switcher-gpt-5-6-thinking] → 验证 span Extended 存在 | 8s | {"extended":true} |
| 3 | 注入提示词 | PowerShell 读文件 → base64 → eval insertText 到 #prompt-textarea[contenteditable=true] → 校验 len 与尾部 | 5s | len>0 且尾行完整 |
| 4 | 发送 | eval 点 [data-testid=send-button]（disabled=false 才点） | 2s | "sent" |
| 5 | 等待+提取 | **自适应轮询（2026-08-28 实测定稿，取代固定 sleep）**：`node scripts/gpt_wait_extract.mjs --session gptreview --out <存档路径>` | 40–180s | 输出 `STABLE & DONE` |

## 实测补充（2026-08-28 阶段3 复核实跑验证；与下表冲突时以本节为准）

1. **`gptreview` 不在 `opencli profile list` 输出里，但确实存在** —— 该命令只列 `n8hh7hyn`（其他 Agent 的共享会话）。**不要据此判定 gptreview 不存在**，直接寻址即可（`browser gptreview state` 正常返回）。误判会退回 n8hh7hyn 与他人争用。
2. **在线判据看 `editor:true`，不看 `interactive` 计数** —— 实测 vip-12 落地后 state 显示 `interactive: 0`，但编辑器已就绪（`editor:true` / `textarea:true`），可直接注入。`interactive:0` 只是 SPA 元素树未刷新。
3. **vip-12 在 gptreview 下 `histLinks=0`**（`a[href*="/c/"]` 计数为 0，等 6 秒复查仍为 0）—— 直接按操作手册 §9.1 例外条款**新开对话**，不要等待或反复重登。
4. **新对话的 `/c/<uuid>` 在发送成功后才出现**，发送前一直是根路径，属正常，不要据此判定未进入会话。
5. **发送成功的硬判据是 `userMsgs>=1` 且 URL 变为 `/c/<uuid>`** —— 仅凭 `sent` 返回值不足以证明消息真的发出（历史误报正源于此：脚本返回 sent 但页面 `userMsgs=0`）。

## 第 5 步详解：为什么必须用自适应轮询（2026-08-28 实测，含事故复盘）

**固定 `sleep 150s` 有两个已实测确认的缺陷，禁止再用：**

1. **会抓到流式中间态，导致存档丢内容（高危）**。阶段3 第 1 轮复核实测：150s 时探测 `len=1449`，而该消息最终长度为 **3858 字符** —— 若按固定等待立即提取，将丢失约 **2/3 的正文**（含后半段判据与总结论）。该轮存档之所以完整，纯属探测脚本与提取脚本之间有时间差侥幸命中，**不是流程保证**。
2. **慢**。用户实测反馈 GPT 有时 30 秒内即答完，固定等 150s 属无效等待。

**自适应轮询的判据（已固化进 `scripts/gpt_wait_extract.mjs`）：**

- **完成判据 = 连续两次探测 `len` 相同 且 `stop=false`**。不再依赖 `assistantCount` 比较——实测发现 baseline 若在发送后采集，会包含新消息的空占位，导致 `count > baseline` 永不成立。
- **自适应间隔**：首次等待 30s；一旦探测到 `len>0`，间隔缩短为 10s 以加速收敛。
- **长思考保护**：Extended 模式下前 60s 常为 `len=0` 的思考期，**这不是空回复**。须连续 6 次探测（约 180s）全为 0 才判定疑似故障。
- **上限**：默认 14 次探测后超时，按故障处置上报用户。

**实测数据**（2026-08-28，vip-12 连通性测试）：

```
[30s]  stop=false count=4 len=0     ← 思考中，不是空回复
[60s]  stop=false count=4 len=0     ← 思考中
[90s]  stop=false count=4 len=6     ← 内容出现
[100s] stop=false count=4 len=6     ← 连续两次一致 → STABLE & DONE
```

总耗时 **100s**，较固定 150s 节省约 50s，且内容完整性由判据保证。

**用法**

```bash
node scripts/gpt_wait_extract.mjs --session gptreview --out <存档路径.txt>
# 可选：--first 30（首次间隔）--fast 10（发现内容后间隔）--max 14（探测上限）
```

退出码：`0`=成功已存档；`1`=超时；`2`=探针失败；`3`=提取失败；`4`=疑似空回复（上报用户，不自动兜底）。

## 快速检查清单（发送前 10 秒过一遍）

- [ ] 发送后是否复核了 `userMsgs>=1` + URL 变 `/c/<uuid>`（不只是 sent）？
- [ ] 提示词是否**内嵌**了所有 GitHub 链接内容（多链接=Deep Research 空回复）？
- [ ] 是否含验证令牌要求（强制 GPT 实读 GitHub）？
- [ ] 轮次：本任务 ≤3 轮？本窗口累计 <12 轮？
- [ ] 凭证检查：提示词里无 key/token/JWT？
- [ ] JSON 注入用文件路径而非 PowerShell 直传？

## 故障处置（每步的异常出口）

| 症状 | 判定 | 处置 |
|---|---|---|
| state 报 Browser Bridge not connected | 桥断 | 让用户在 Chrome 重载 opencli 扩展；或 daemon restart 后等 30s 重试一次 |
| 页面 about:blank | 会话空 | browser gptreview open https://vip-12.67673.live/ + sleep 10 |
| Auto pill 找不到 | 未登录/页面未加载完 | state 看 URL；重 open；再不行换账号重登（问达宝读 panelAccountList → localStorage authToken → plus-login URL） |
| 发送后 assistant 0 字（连续 2 次） | 实例故障 | **报告用户**：现象+已试账号+建议，不自动兜底（D-GLOBAL-20260828-01） |
| eval 报 Target tab not part of session | 标签失效 | tab new "URL" 开新标签拿新 target id；绝不复用旧 id |
| GitHub 内容对不上 | 页面缓存 | 汇报附 commit 短 SHA + raw.githubusercontent.com 直链 |
| PowerShell 传中文 JSON 乱码 | 编码 | --json @相对路径.json（UTF-8 无 BOM，先 cd 到目录） |

## 会话记录批量管理（详见操作手册01 §10）

- 脚本：C:\Users\郭永涛\.openclaw-autoclaw\workspace\.openclaw\tmp\mirror_cleanup.py
- dryrun → 人工核对 → exec（自动备份+分批删除+台账）→ 需要时 restore <备份>
- **实测警告**：镜像站删除=硬删除，恢复仅部分有效（实测 2/10）；备份 JSON 是唯一依据，消息正文不可恢复。

## 否定性判据补证标准（GPT 阶段3 复核裁定确立，2026-08-28）

**规则**：凡「未泄漏 / 未修改 / 零访问」这类**否定性判据**，必须配机器可验证的 scan / diff / count，**不能只写执行者结论**。单凭"我确认没有"一律判证据不足。

**工具**：记忆仓 `scripts/secret_scan.py`（已入库）

```bash
# 判定「是否泄漏进 commit/记忆/日志」——用 tracked-only，这是正确范围
python scripts/secret_scan.py --tracked-only \
  --repo "D:/项目" \
  --state "D:/ai-hub-memory/projects/devel-tools/STATE.md" \
  --ccswitch-log ~/.cc-switch/logs/cc-switch.log \
  --range 8ec04de1..HEAD --out <报告路径.json>
```

- 覆盖：Git 已跟踪文件 + `git log -p <range>` 全部提交 diff + STATE.md + CC Switch 日志
- 7 类规则：openai_key / anthropic_key / jwt / aws_akid / bearer / private_key / env_assign
- 强制 redaction：**只输出 finding_count 与 exit_code，绝不保存命中的秘密正文**
- 达标线：`finding_count=0` 且 `exit_code=0`

**关键排雷**：全量工作树扫描会命中设计内的本地凭证存储（`data/search_gateway/channels.json`、`api_state.json`、`ai-resource-hub/scheduler/credentials.json` 等）。这些文件是 **untracked + gitignored**，属网关/调度器运行必需的本地凭证信任域，**不构成泄漏**。判定前必须用两条命令验证其性质：

```bash
git ls-files --error-unmatch <path>   # 应报未跟踪
git log --all --oneline -- <path>     # 应为空，证明从未进入历史
```

**GPT 的边界口径**：判据要证明的是"没有泄漏进日志 / 记忆 / commit"，**不是**要求运行中的网关完全不能持有本地凭证。阶段4 起继续维持：credential_ref 可进配置/控制平面，真实 secret 只留本地凭证信任域。

## 台账字段（每次问诊记录到 devel-tools STATE）

- 字段：SID、日期、会话（gptreview/vip-12/新窗口第 N 轮）、提示词文件路径、GPT 结论摘要、验读令牌值、存档路径。
- 负责人：当值 Agent。
- 检查频率：每次问诊后即时写；每阶段结束核对 STATE 与实际文件一致性。
- 异常处理：按本文「故障处置」表；处置结果写进当日 STATE 条目。
- 团队检查方式：STATE 条目 ↔ 存档文件逐条对照，任何人可抽查。
