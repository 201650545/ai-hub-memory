# 任务转交指令（2026-08-28 夜间 → 下一棒 Agent）

> 交接背景：本棒因会话成本较高而转出。以下内容自包含，读完即可接手。
> 交接时点：记忆仓 `d11a636`、代码仓 `728439e`（两仓本地=远端）
> **接手第一步务必先 `git pull --ff-only`**：本棒工作期间有并行 Agent 持续提交（见第三节重要更正）

---

## 一、任务全景与当前位置

两条主线并行，都未完成：

| 主线 | 当前位置 | 卡在用户？ |
|---|---|---|
| A. AI-Hub :3100 网关阶段 0–6 | 阶段0–3 已完成；**阶段4 设计评审已拿完整方案，未开工** | ✅ 卡 Step 0 授权 |
| B. 记忆系统分层/封存/唤醒架构 | **Phase M0+M1 已完成并五项验收 PASS**，待 M2 | ❌ 可直接推进 |

另有并行 Agent 在 `ai-resources` 线做渠道计费核查，持续产出并与本线交叉。

---

## 二、本棒已完成（勿重复做）

1. **阶段3 十二判据复核 → 整体 PASS 12/12**（GPT Extended 两轮裁定，令牌实读验证通过）。存档：`D:\项目\ai-resource-hub\docs\ai-advice\gpt56_阶段3十二判据复核_2026-08-28.md`、`gpt56_阶段3补证复核裁定_2026-08-28.md`
2. **阶段4 资源控制平面设计评审** → 14385 字方案（含 Step 0 blocker + 12 条验收判据）。存档：`gpt56_阶段4资源控制平面设计_2026-08-28.md`
3. **记忆系统「分层+封存+唤醒」架构方案** → 10689 字。存档：`gpt56_记忆系统分层封存唤醒架构_2026-08-28.md`
4. **Phase M0**：`scripts/memory.py` 新增只读 `tier-plan --project <p> --dry-run`，零文件移动
5. **Phase M1（试点 devel-tools，五项验收全 PASS）**：新建 `projects/devel-tools/MEMORY.md`（稳定知识层）+ `ROUTER.md`（冷记忆路由索引）；逐字迁移 2 条到 `archive/projects/devel-tools/2026/2026-08.md`，**checksum 前后一致**（`d7afced3d08f39b7` / `5cbcc5d2e9a4a785`）
6. **数据安全加固**：`scripts/backup_memory.py` + 每日 21:00 自动备份（automation-1787936803135）
7. **`channels.py` 悬置状态已了结**：另一 Agent 提交 `728439e`，本棒推送
8. **问题库已建**：`docs/累计问题与解决方案库_2026-08-28.md`（17 条，含 4 条待 GPT 裁决）

---

## 三、⚠️ 重要更正：扣费风险结论已被补充/部分推翻

**本棒曾给出「Fast 编排无扣费路径」结论。该结论只在"fast 五成员"范围内成立，范围外存在真实扣费路径。**

并行 Agent 后续发现（ai-resources S-20260829-05）：

1. **ZenMux 并非免费渠道**：目录 166 款中**仅 5 款价格全为 0**，其余 161 款收费。渠道定义 `billing_tag` 笼统写「🟢 免费」是**错误口径**。
2. **真实扣费路径**：190 个可见模型名中 **12 个会解析到 zenmux 收费款且 eligible=True**，如 `claude-sonnet-5`、`gpt-5.6-sol`、`glm-5.2`、`hy3`、`mistral-large-2512`。
3. **网关无"仅免费"闸门**：`channels.py` 的 `free_models` 只写健康状态（第 930 行）不参与路由判定，无 `free_only`/`block_paid` 开关。**需新增功能**，按纪律须先出设计并过 GPT Extended 评审，目前仅记 backlog、未动代码。
4. **失效配置**：`deepseek/deepseek-v4-flash-vision-exp-free` 已下架。

**影响本线 M2 的坑**：`channels.set_unified_model` 是**重建条目**而非合并，只写 members 与 display，会**丢弃 tier 字段**。正确顺序是「先 POST 拿热加载与 DSH 同步，再回填 tier」，否则破坏 tier-plan 依赖的分层逻辑。

**飞书 Base 已被并行重构、表 ID 失效**：「模型白嫖渠道」`tbl40PAWYub8G5m8` **已不存在**（写入报 `800030104 not_found`）；现模型渠道数据在「模型资源总表」**`tbl5ONs0gzE7I5xI`**（90 条）。

---

## 四、第一动作（接手后先做）

**Phase M2：把记忆分层架构扩展到其余项目线。** 但建议先做两件小事，避免把问题复制到 7 个项目：

1. **修 `projects/devel-tools/STATE.md` 重复章节**：「## 已完成（最近）」标题重复两次（第 6 行与第 49 行）。
2. **复核分类规则**：protected 占 48.1% 偏高，主因「卡点」常指"当时遇到但已解决"却被判为活跃约束。建议拆为「当前未决卡点」（保护）/「历史已解卡点」（可归档）。

然后**逐项目扩展，每个项目一个独立 commit，不要一次改七个**。

命令：
```
cd D:\ai-hub-memory
git pull --ff-only
python scripts/memory.py tier-plan --project <项目线> --dry-run
```
项目线：`ai-resources`（最新最活跃，优先）、`courseware`、`english-learning`、`english-teaching`、`memory-system`、`nitian-theme`、`teaching`

---

## 五、必须用户授权才能动（勿擅自开始）

1. **阶段4 Step 0**：`ai-resource-hub` 两个分叉副本 `D:\ai-resource-hub`(f55ec94) 与 `D:\项目\ai-resource-hub`(b9d8656)。GPT 明确：**唯一真源确定前不允许启动控制平面定时任务**。
2. **免费闸门功能**：新功能，须先出设计并过 GPT Extended 评审。
3. **夸克网盘备份环**：官方 skill 无上传能力，`qk-list` 只能列根目录，需用户扫码。
4. **飞书**：用户明确指示**暂停**（另一 AI 正在重构多维表格）。

---

## 六、强制纪律（违反即作废）

1. **标签页纪律**：只开新标签（`tab new`），只关自己开的。
2. **发送前探活是硬门槛**：成功判据 = `userMsgs>=1` **且** URL 变为 `/c/<uuid>`，仅凭 `sent` 不算（历史事故：死标签空转）。
3. **等待提取用稳定性判据**：连续两次 `len` 相同且 `stop=false`，用 `scripts/gpt_wait_extract.mjs`。**固定 sleep 150s 会抓中间态丢内容**（实测 150s 时 1449 字符，最终 3858）。
4. **问诊同频**：问诊前必须 commit + push 并验证 raw 返回 200，否则 GPT 按核验规则中止作答。
5. **每阶段必须 GPT Extended 评审** + 实读令牌（`P3PASS-FAILOVER-EVIDENCE-9RB6-XD5M-VERIFIED`）。单任务 ≤3 轮、单窗口 ≤12 轮。
6. **凭证绝不入提示词/记忆/commit**，只写"是否存在"。调试输出也要脱敏。
7. **禁 `git push --force`**；冲突先 `pull --ff-only` → `rebase` → 再失败停下报告。
8. **他人未提交改动不代提交、不代回退**（`D:\项目\scripts\`、`watchdog_gateway.bat` 属他人）。
9. **镜像站故障直接报告用户**，不自动换 Kimi 兜底（已停用）。
10. **动手前先查是否已有机制**（教训：提议做 DSH 动态同步，实际 `sync_dsh_models()` 早已存在）。

---

## 七、资源清单

| 用途 | 路径 |
|---|---|
| 记忆仓 | `D:\ai-hub-memory` = github.com/201650545/ai-hub-memory（master） |
| 代码仓 | `D:\项目` = github.com/201650545/ai-hub @ `refactor/monorepo-20260812`（`728439e`，35/35） |
| 问诊手册 | `docs\镜像站问诊固化SOP_2026-08-28.md` |
| 问题库 | `docs\累计问题与解决方案库_2026-08-28.md` |
| 阶段4 方案 | `D:\项目\ai-resource-hub\docs\ai-advice\gpt56_阶段4资源控制平面设计_2026-08-28.md` |
| 记忆架构方案 | `D:\项目\ai-resource-hub\docs\ai-advice\gpt56_记忆系统分层封存唤醒架构_2026-08-28.md` |
| 工具 | `scripts/memory.py`(含 tier-plan)、`secret_scan.py`、`backup_memory.py`、`gpt_wait_extract.mjs` |
| 浏览器 | opencli 会话 `gptreview`（不在 profile list 但可直接寻址）；健康账号从 `panelAccountList` 现读并**池内轮转** |
| 网关 | `:3100`（API 聚合）、`:3000`（多引擎 AI 搜索） |
| 飞书 Base | `StmDbTXQWaujshs9NpIc3UFpnAc`，模型资源总表 `tbl5ONs0gzE7I5xI` |

---

## 八、试读自测（答不出请重读）

1. 第一动作？（先修重复章节+复核分类规则，再做 M2 逐项目扩展）
2. 三层正文与 ROUTER 禁区？（MEMORY 稳定知识/STATE 当前状态/archive 历史过程；ROUTER 只存路由元数据，**不写结论**）
3. 为何"冷"≠"可信度低"？（权威状态与激活温度是两个正交维度）
4. 扣费风险真实状况？（fast 五成员确 0 价；ZenMux 166 款仅 5 款免费；12 个模型名有真实扣费路径；网关无免费闸门）
5. 发送与提取判据？（`userMsgs>=1`+URL 进 `/c/`；连续两次 `len` 相同且 `stop=false`）
6. 哪些须授权？（阶段4 Step 0、免费闸门新功能、夸克、飞书）
7. 他人在改的文件？（不代提交、不代回退、不覆盖）
8. 问诊引用文档前？（commit+push 并验证 raw 200）
