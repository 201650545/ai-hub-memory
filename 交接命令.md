# 交接命令 —— 直接复制给任何 Agent 用

> 用法：把下面 `---` 之间的整段文字，直接发给要执行任务的 Agent（ChatGPT / Kimi / 执行 AI / 另一个 Claude Code 会话）。它会在开工前读记忆、交付后写记忆，全程按「项目单元」粒度判断，不每次对话都读写。

---

你现在接手一个项目任务。开工前，请先按下面的规则做**记忆同步**，再开始干活；交付后再同步一次。

## 读（只在「新项目单元」开始时读一次）

1. 先拉取共享记忆仓库：
   ```
   gh repo clone 201650545/ai-hub-memory
   ```
   （如果本地已有，就 `git pull` 到最新。）

2. 只读这两个文件（一页纸，很快）：
   - `STATE.md` —— 当前项目进度 / 卡点 / 下一步
   - `DECISIONS.md` —— 用户已经拍板、不许推翻的决策

3. **判断要不要读**：这是不是一个新的、独立的任务？是 → 读；只是上一个任务的延续 / 追问 → 跳过，直接干活。

## 写（只在「某个可交付单元完成后」写一次）

任务做完一个能独立验收的部分后，再同步一次：

1. `git pull` 到最新（防止和别人冲突）。
2. 在 `CHANGELOG.md` 末尾**追加**一条：`- <你是谁>：<何时> <做了什么>`。
3. **整体重写** `STATE.md`，反映最新状态（进行中 / 已完成 / 卡点 / 下一步）。
4. 如果用户**敲板**了某个决策，在 `DECISIONS.md` 末尾**追加**一条（带日期）。
5. `git add -A && git commit -m "..." && git push`。

4. **判断要不要写**：这个成果「别的 Agent 会用到 / 需要知道」吗？是 → 写；纯中间过程 → 跳过。

## 三条铁律

- 新项目开始前读一遍，一个单元交付后写一条。宁读不重写。
- 凭证 / key 绝不写进任何记忆文件或 commit。
- 改记忆前先 `git pull`，避免覆盖别人。
---

（完）
