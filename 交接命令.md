# 交接命令 —— 直接复制给任何 Agent 用

> 用法：把下面 `---` 之间的整段文字，直接发给要执行任务的 Agent（ChatGPT / Kimi / 执行 AI / 另一个 Claude Code 会话）。它会在开工前读记忆、交付后写记忆，全程按「项目单元」粒度判断，不每次对话都读写。

---

你现在接手一个项目任务。开工前，请先按下面的规则做**记忆同步**，再开始干活；交付后再同步一次。

## 读（只在「新项目单元」开始时读一次）

1. 先拉取共享记忆仓库：
   ```
   gh repo clone 201650545/ai-hub-memory
   ```
   （如果本地已有，就 `git pull --ff-only` 到最新。）

2. 先确定**当前项目**（教学 teaching / 课件 courseware / 记忆系统 memory-system）：
   - 读 `MEMORY.json` 路由表 + `global/RULES.md`（9 条宪法）+ `global/DECISIONS.md`（全局决策）
   - 读 `projects/<当前项目>/STATE.md`（进度/卡点/下一步）+ `projects/<当前项目>/DECISIONS.md`（项目决策）
   - 行为协议见 `skills/memory-router/SKILL.md`

   - **如果是新项目**（MEMORY.json 里没有）：不用先建文件，正常干活，只读 global/；任务做完确定是新项目后，运行 `python scripts/memory.py register --id <英文id> --name <中文名> --aliases <别名>` 一键建记忆线。

3. **判断要不要读**：这是不是一个新的、独立的任务？是 → 读；只是上一个任务的延续 / 追问 → 跳过，直接干活。

## 写（只在「某个可交付单元完成后」写一次）

任务做完一个能独立验收的部分后，再同步一次：

1. `git pull --ff-only` 到最新（防止和别人冲突；历史分叉会直接停下，不要偷偷 merge）。
2. **重读磁盘上的最新 `projects/<当前项目>/STATE.md`**，禁止按 Agent 旧上下文整页重建（防语义覆盖，最重要）。
3. 所有写操作走 `python scripts/memory.py write --project <当前项目> --kind state|decision --sid <S-ID> --content <内容>`（脚本自动追加 CHANGELOG）。
   - **批量同步自己的记忆**：如果你有多个记忆条目/文件要导入，用 `python scripts/memory.py sync --project <当前项目> --dir <目录>`（自动判断 state/decision、生成 ID、写 CHANGELOG）；单条用 `--file <文件>`。
4. **保留式更新** `STATE.md`：
   - 稳定 ID（`[S-xxxx]`）必须保留，正常更新只改文字；
   - 要删除某条 → **先在 CHANGELOG 追加 `DROP <ID> — 原因`**，再从 STATE 删除（pre-commit hook 会拦截无 DROP 的删除）；
   - 误删找回 → `git show <GOOD_COMMIT>:STATE.md | grep "S-xxx"`，插回并追加 `RESTORE <ID>`。
5. 如果用户**敲板**了某个决策，在 `DECISIONS.md` 末尾**追加**一条（带日期）。
6. `git add -A && git commit -m "..." && git push`。
   - **禁止** `reset --hard` + `force push`（改写共享历史）；整提交撤销用 `git revert`，单文件恢复用 `git restore --source=<commit> -- <file>`。
   - push 失败（远端前进）→ 重新 `git pull --ff-only` → 重读最新记忆 → 重新合并 → 再 push。

7. **判断要不要写**：这个成果「别的 Agent 会用到 / 需要知道」吗？是 → 写；纯中间过程 → 跳过。

## 三条铁律

- 新项目开始前读一遍，一个单元交付后写一条。宁读不重写。
- 凭证 / key 绝不写进任何记忆文件或 commit（pre-commit hook 会机械拦截；误入历史 = 废弃 key，不是删文件）。
- 改记忆前先 `git pull --ff-only`，避免覆盖别人。
---

（完）
