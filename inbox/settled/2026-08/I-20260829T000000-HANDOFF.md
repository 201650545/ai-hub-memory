---
schema: inbox-v1
id: I-20260829T000000-HANDOFF
captured_at: 2026-08-29T00:00:00+08:00
capture_scope: handoff
project_hint: ai-resources
routing_basis: D-GLOBAL-20260829-01
kind_hint: execution
---

# 转交任务：API 转发网关收尾核查（Fast 编排 + 渠道计费）

## 接手方
本地快速执行 Agent（负责浏览器操作 / 网关配置 / 编排，豆包侧只做搜索+方案+上传飞书）

## 任务背景与位置（从哪里找）

**本地 API 转发网关**：`http://127.0.0.1:3100`（`api_gateway.py`，AI Hub 网关）
- 配置目录：`D:\项目\data\search_gateway\`（routing.json / unified_models.json / channels.json / channel_models.json）
- 编排接口：`GET /api/unified`（运行中网关的编排）；`/v1/models`、`/api/channels`

**飞书 Base**：`https://my.feishu.cn/base/StmDbTXQWaujshs9NpIc3UFpnAc`，「模型白嫖渠道」表（tbl40PAWYub8G5m8）

**记忆/规范**：GitHub 仓库 `201650545/ai-hub-memory`（global/TOOLS.md 分工边界 + DECISIONS D-GLOBAL-20260829-01）

## 要做什么（未完成任务清单）

当前网关「模型编排」已精简为**只保留 `fast` 一个编排组**（5 成员：openrouter/agnes/zenmux/sensetime/xiaohongshu）。还有以下 3 项未收尾，请完成：

1. **小红书模型速度核查**：`fast` 编排中 xiaohongshu 渠道的 `dots3-note-prev` 之前速度很快，现在实测超时/慢。请核查 xiaohongshu 渠道当前健康状态、最近调用延迟，判断是暂时不可达还是模型下架/改版，并给出处理（恢复 / 降级 / 移除）。

2. **Agnes 2.5 Pro 计费核查**：用户确认「Agnes 2.5 Pro 是要收费的」。请核查 `fast` 编排里 agnes 成员用的是免费版（agnes-2.5-flash）还是收费版（agnes-2.5-pro），确认当前是否会被扣费。

3. **Fast 编排扣费风险规避**：确保 `fast` 编排实际路由走的都是免费档模型，**绝不能让用户被扣费**。若发现任何成员会扣费，调整为只用免费模型。

## 完成标准
- 3 项均实测核查完毕，给出证据（渠道状态、具体模型名、是否免费、调用结果）。
- 网关配置与飞书 Base「模型白嫖渠道」表同步更新（按规范）。
- 结果按 D-GLOBAL-20260829-01 规范上传（能上传飞书的传飞书，并同步记忆）。

## 备注
- 只操作网关配置和飞书表，不碰用户标签页。
- 遇到需用户登录/账号操作/身份信息时，停下来等用户，不擅自代做。
- 若需浏览器操作，遵守标签页规范（新开标签、只关自己开的）。
