# DECISIONS.md — 全局决策（跨所有项目，append-only）

> 只追加。这里只放「删除任何一个项目后依然成立」的决策。项目专属决策在各项目的 DECISIONS.md。

## 2026-08-29
- [D-GLOBAL-20260829-01] **豆包/资源搜索 Agent 职责边界收敛**（用户 2026-08-29 拍板，强制）：豆包侧 Agent 职责收敛为**信息搜索 + 资源搜索 + 结合最新信息做方案 + 上传飞书**；**不再负责**本地浏览器操作（打开 Chrome/登录/点击）、本地系统/网关配置、模型编排等需结合本地浏览器的执行类工作——此类工作派给专门的快速 Agent（如执行 Agent）去做，做完由它们按规范上传到合适位置。同步修订：global/TOOLS.md 分工边界、README.md 分工。
- [D-GLOBAL-20260829-02] **问诊/评审主渠道固定为 GPT 镜像站，Claude 不再作默认兜底**（用户 2026-08-29 拍板，强制）：① **默认且主力**是 GPT 镜像站（Thinking·Extended，会话纪律沿用 D-GLOBAL-20260815-03）；② Claude 额度稀缺，**不得**因镜像站故障就自动改用 Claude 评审——转 Claude 必须先经用户明确同意，且一次授权只覆盖当次；③ 镜像站故障时的处置沿用 D-GLOBAL-20260828-01：向用户报告故障（现象、已试账号/会话、建议动作），**等指示**，不自行更换评审模型；④ 建议动作优先给「换账号/换会话/稍后重试同一 GPT 实例」，因为退化通常是实例侧而非流程侧。先例：2026-08-29 价格闸门 v0.2 评审遇 GPT 同实例三轮退化（19/85/5 字符），Agent 按纪律上报未擅自兜底，用户当时指定 Claude Sonnet 5 High 兜底一次——本条把该次例外收回，恢复 GPT 为主的常态。同步修订：global/TOOLS.md 镜像站故障处理、docs/镜像站问诊固化SOP_2026-08-28.md 故障处置表。

## 2026-08-28
- [D-GLOBAL-20260828-01] **标签页管理规范**（用户 2026-08-28 拍板，强制，适用所有用 opencli/浏览器操作的 Agent）：① 由 AI 打开的标签页，任务完成后即关闭该标签页；② 新任务 / 不同类型任务时开新标签页，做完关闭旧的；③ **只关闭 AI 自己打开的标签页，绝不关闭用户或其他 Agent 打开的标签页**。同步修订：global/TOOLS.md 浏览器操作纪律。
- [D-GLOBAL-20260828-02] **CC Switch takeover 授权边界**（GPT-5.6 Extended 裁定 + 用户 2026-08-28 批准升格，强制）：CC Switch v3.14.1 启用本地 proxy 即自动改写 `~/.claude/settings.json` 并接管 Claude Code 出口，故必须视为「修改全局客户端配置」级别的高危操作——未经授权不得 enable/re-enable，且 `live_takeover_active=0` 不得当作未接管的依据；获批操作必须先存可验证回退基线、任务结束即恢复原态，永久切流需另行授权。同时固定职责边界：CC Switch=协议转换 + Claude 模型槽位适配，:3100=unified model→能力/容量/渠道路由，Claude Code=工具执行与对话状态；不得在 :3100 新增 `/v1/messages`，不得让网关重新接受 `<渠道>:<模型>` 前缀（那等于把渠道选择权漏给客户端）。同步修订：global/RULES.md 问诊约定、global/TOOLS.md §6.7。
- [D-GLOBAL-20260828-03] **问诊同频与实读证明**（用户 2026-08-28 拍板，强制）：每完成一次镜像 GPT 问诊，先把本轮结论/状态变更 push 到 GitHub 记忆仓库再问下一轮，保证 GPT 与仓库同频；问诊包必须要求 GPT 实读仓库内容，并内嵌一个只有真读文件才能获得、无法从上下文猜出的核验令牌令其原文引用，引用不出即判未实读、本轮作废。同步修订：global/RULES.md 问诊约定。

## 2026-08-28
- [D-GLOBAL-20260828-01] **停用 Kimi K3 兜底**（用户 2026-08-28 拍板，SUPERSEDES D-GLOBAL-20260815-04 的兜底路径）：GPT 镜像站出问题时不再自动转 Kimi K3，而是直接向用户报告故障（现象、已尝试的账号/会话、建议动作），等用户指示。Kimi K3 仍可作为用户主动要求的工具使用，但不再承担「自动兜底」角色。

## 2026-08-28
- [D-GLOBAL-20260828-01] **停用 Kimi K3 兜底**（用户 2026-08-28 拍板，SUPERSEDES D-GLOBAL-20260815-04 的兜底路径）：GPT 镜像站出问题时不再自动转 Kimi K3，而是直接向用户报告故障（现象、已尝试的账号/会话、建议动作），等用户指示。Kimi K3 仍可作为用户主动要求的工具使用，但不再承担「自动兜底」角色。

## 2026-08-27
- [D-GLOBAL-20260827-01] **GPT 镜像站窗口轮次上限 42 → 12**（用户 2026-08-27 拍板，修订 D-GLOBAL-20260815-03 第③条）：一个对话窗口最多进行 **12 轮**对话，达到 12 轮必须新开对话（新开后重新选 Thinking·Extended）；每单个任务仍须在 **≤3 轮**对话内完成（原第②条语义不变，表述由「每个问题」改为「每个任务」）。复用历史会话与 Extended 模式要求不变。同步修订：global/TOOLS.md 会话纪律、ai-resource-hub docs/操作手册/01 §9。

## 2026-08-15
- [D-GLOBAL-20260815-01] **自有知识解决不了 → 转问 GPT**：当 Agent 对上游行为/资源/架构/外部服务现象判断不定、或已自行尝试多次仍无把握时，不要埋头硬试，转 GPT 镜像站（Thinking·Extended）问诊。典型场景：upstream 403/报错、免费档/价格判断、模型名/鉴权方式、某厂商资源的可用性确认。GPT 镜像站故障时转 Claude 兜底（沿用 D-20260813-04）。
- [D-GLOBAL-20260815-02] **代表用户执行**（用户 2026-08-15 拍板，对 D-GLOBAL-20260813-03 的项目内例外）：用户不懂技术细节，Agent 代表用户处理所有技术决策，不逐项向用户确认技术问题；拿不准的架构/资源/外部行为判断转 GPT 镜像站（Thinking·Extended）问诊，问诊轮次≤3 轮须收敛；GPT 回复后自主选用最推荐方案直接落地执行；完成只告知用户「项目已可用」，不汇报技术细节。
- [D-GLOBAL-20260815-03] **GPT 镜像站会话纪律**（用户 2026-08-15 拍板，强制）：① 进入后复用左侧历史会话（点历史记录进入，不新开对话），再调模型为 Thinking·Extended；② 每个问题与 GPT ≤3 轮对答解决（含 GPT 反问的交流，3 轮内必须解决回来执行方案）；③ 每窗口 ≤42 轮，超过必须新开对话（新开后重新选模型）。详细步骤见 ai-resource-hub 操作手册 01 §9 与 global/TOOLS.md §4。
- [D-GLOBAL-20260815-04] **问诊渠道变更：Claude 停用，兜底改 Kimi K3**（用户 2026-08-15 拍板）：Claude 额度已用光，问诊不再转 Claude（SUPERSEDES D-GLOBAL-20260813-04 与 D-GLOBAL-20260815-01 中「GPT 故障转 Claude」的兜底路径）。新路由：① 优先 GPT 镜像站（Thinking·Extended，会话纪律沿用 D-GLOBAL-20260815-03）；② GPT 不可用或需第二意见时 → **Kimi K3**：opencli 打开 Kimi 官网（kimi.com），提问前开启 **K3 思考进阶模式**；③ 以上全部经 **opencli** 操控浏览器执行。

## 2026-08-14
- [D-GLOBAL-20260814-01] 记忆系统 v2 = **项目作用域隔离 + 分层记忆 + Routing-before-Retrieval + Fail-Closed**（三方交叉定稿：AI 搜索网关 + Claude Sonnet5 + GPT-5.6 Extended）。核心公式：Memory = Global Kernel + Project Namespace + Layered Retrieval；Multi-read / Single-write；scope 不明确拒绝读写。落地：MEMORY.json + memory.py + memory-router/SKILL.md。

## 2026-08-13
- [D-GLOBAL-20260813-01] 工作模型 = **按需调度**：用户自然说话，Agent 当调度大脑路由已打通工具；**不做**定时提醒 / 汇报平台。
- [D-GLOBAL-20260813-02] 多 Agent 共享记忆 = **GitHub 仓库承载**（唯一真源），记忆以项目为原子单元。
- [D-GLOBAL-20260813-03] 每次执行前先向用户确认。
- [D-GLOBAL-20260813-04] 问诊走 GPT 镜像站 **Thinking · Extended**（不是 Auto）；GPT 镜像站故障时转 Claude 兜底。

## 2026-08-12
- [D-GLOBAL-20260812-01] 前端统一苹果浅色风 + 深色切换按钮。