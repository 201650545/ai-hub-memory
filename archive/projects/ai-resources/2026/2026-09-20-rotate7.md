# 2026-09-20 STATE 轮转（第七次）— S-20260903-13 下沉

> 来源：STATE.md「已完成(最近)」超 8 上限（腾位 S-20260920-06，2026-09-20）。
> 本文件为归档快照，不可修改（archive files immutable）；当日再次轮转须新建文件。

## S-20260903-13 RFC v2 P1 落地（B′ sensetime 动态升权；Cloudflare 热备机制就绪待凭据）

**B′** 在 `search_gateway\fault_domains.py` 新增 `any_proxy_tripped()` + `promote_on_proxy_down(chain)`（`any_proxy_tripped` 时把配置 `promote_channels` 里的**直连**渠道升链首，恢复按实时 trip 自动回落，正常态零改动；只升直连、代理渠道跳过），`DEFAULTS` 补 `promote_channels=[]` 键（否则被 config() 白名单丢键），注入 `api_gateway.py:route_completion` 链构建后。配置 `data/search_gateway/fault_domains.json` `promote_channels=["sensetime","cloudflare"]`。进程内验证 4 断言全过：正常原链、熔断时 sensetime 升 #1、恢复回落、代理渠道不升权；重启(PID 15564→50724)后实时冒烟无回归（fast→dots3-note-prev 200、deepseek-free→MiniMax-M3 200）。**Cloudflare Workers AI 直连热备**：网关侧机制已就位（无 proxy 直连自定义渠道=独立故障域，命名 cloudflare 即自动纳入热备），只差郭老师填 Workers AI 凭据，注册模板已写入 RFC 计划文档 §Cloudflare。RFC 计划文档待办更新为：P0 全落地、P1 B′ 落地、P1 CF 仅剩凭据。（2026-09-03，调度大脑）
