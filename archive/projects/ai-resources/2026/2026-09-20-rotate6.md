# 2026-09-20 STATE 轮转（第六次）— S-20260903-12 下沉

> 来源：STATE.md「已完成(最近)」超 8 上限（腾位 S-20260920-05，2026-09-20）。
> 本文件为归档快照，不可修改（archive files immutable）；当日再次轮转须新建文件。

---

- **[S-20260903-12]** RFC v2 P0 两项落地（F+G）：**F 故障域熔断** → 新增 `search_gateway\fault_domains.py`（按渠道 proxy 字段域分组、反应式 trip 短路整组、`request_deadline_s=30` 封死链），配置 `data/search_gateway/fault_domains.json`；注入 `api_gateway.py:route_completion`（循环前取 deadline、:587 前 `is_tripped` 短路、代理渠道 TIMEOUT `mark_failure`、成功 `mark_success`）。**G capability contract test** → 新增 `capability_verify.py`（复用 `channels._urlopen` 但对 default_model 跑 chat/vision/tools 三测，结果写回 `model_capabilities.json` fail-closed），`channels.py:save_custom_channel` 注册后 `trigger_async` 惰性 import 异步触发。验证：转发放通无回归（deepseek-free→MiniMax-M3 200）；死渠道注册被写 `chat:false` 整渠道隔离；假 tools 渠道被 `check_candidate` 排除（mismatch=['tools']）。测试渠道/键与能力条目已清理。P1（B′ sensetime 动态升权、Cloudflare 直连热备）排期未做。（2026-09-03，调度大脑）
