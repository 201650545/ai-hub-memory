# ai-resources STATE 归档（2026-08-30 rotate 全量）

> 全部条目 verbatim，仅保留最新 8 条于 STATE。


- **[S-20260829-28]** 价格闸门 P2 走向定案：改做缩窄版纯观测并已生效（2026-08-29）①ROI 拍板：用户看完真实对账后否掉原 P2 全量 gate 接线，选纯观测。评审 9 项阻断必改整体不适用——它们全部源自 enforce 权限、observe_until 时钟、3106 启动自检与 NSSM env 整块覆盖，本步三样都不碰（api_gateway.py 内 effective_verdict/resolve_mode/validate_startup_config/PRICING_MODE 引用数实测 0）。②代码：代码仓 b5876ea——pricing.py 新增 peek_class()（复用 _entry_class/_is_stale 三级回退与新鲜度降级，无 allow/deny 语义、不读任何 PRICING_* env、异常返回 None）；api_gateway.py 路由日志补 attempted_class/resolved_class，_log_route 成为唯一写入口并双写 data/search_gateway/pricing_telemetry.jsonl，writer 连续失败 20 次自禁用。测试 pricing 51→55 全过，新增 telemetry 守护 5 例全过。③已生效且无需我重启：约 19:13 有人重启过 :3100（register_gateway_service.log 内最后一次 restart_clean 仍是 18:16:53–18:17:01，当前服务 PID 22448≠39124），线上已产出 3 行真实 telemetry；响应头仍只读具名字段（X-Pricing-Class 为 None），响应字节未变。④决定性发现（推翻本窗口我此前两次静态推断）：真实 deepseek-v4-flash 候选链是 11 个成员（ark, siliconflow, bai, modelscope, sensetime, openrouter, dashscope, zenmux, gmi, nvidia, opencode），不是 unified 组里的 7 个；链上 ark/siliconflow/gmi 三个渠道在 model_pricing.json 里完全未登记→按 global_default 记 unknown；而实际服务中的头号渠道正是 ark(unknown)，fb=0。故 enforce 若今天上线，效果不是「省 0 元」而是把主力渠道整体踢出路由、全部 deepseek 流量改道 bai/nvidia。⑤推论：P3 的前置条件不是观察窗而是定价表覆盖度；ark(火山方舟 Agent Plan 套餐内) 与 gmi/siliconflow 的 class 必须先人工核实登记，两周后按 JSONL 判定 P3 取舍。⑥并发提示：api_gateway.py 工作树内另有他人未提交的 content_filter 归一化改动（_normalize_content_filter），已随该次重启上线（19:14:47 err 日志有触发记录），我只提交了自己的 6 个 hunk，未纳入也未回退其改动。（2026-08-29）

- **[S-20260829-27]** 阶段5 控制平面实施关账（用户授权「你都完成啊」延续，租约 C-20260830-01）。①核心落地（评审 S5-PIPELINE-DESIGN-2026 全采纳，镜像 GPT 故障按预案由 Claude Sonnet 5 High 兜底）：state.py/lease.py/sync.py——Q2 单实例文件租约（O_CREAT|O_EXCL+30s 心跳线程+150s 陈旧+原子接管，3110 占用/3111 失租，永不空转）；Q4 三类失败（fetch 指数退避 60×2ⁿ 封顶 1800±20% 抖动；validate 同原始输入哈希 3 连败→halted+3205 仅 --clear-halt 解除，配 pending_vf 强制全量重算防 revision 去重饿死熔断；ACK 超时按 loop 节奏短重试、≥3 连败或 24h 回滚≥6 次→3204 去抖 10min）；Q5 防振荡（noop_same_sha+回滚冷却 5×loop+最小发布间隔 60s）；Q6 重启无特殊逻辑；Q7 观测（state 原子覆写+JSONL 512KB 轮转+alert 文件+事件 3201-3205 源 API3100ControlPlane）；Q8 per-key/渠道 gating 全 defer 维持 channel_name_mapped。27 新单测+62/62 回归，提交 932c6c2+de7aba3。②**方案变更（关键实证）**：NSSM SYSTEM 服务结构性不可行——lark-cli UAT 绑定用户 DPAPI 态存储（config.json 仅 app 凭证+openid 无 token 文件，SYSTEM+USERPROFILE/HOME 覆盖仍 token_missing；cmdkey/PasswordVault 空，openclaw-feishu-uat .enc 为用户态加密），改**登录触发计划任务 ControlPlaneLoop**（pythonw -m control_plane.sync --loop 60 --daemon；pythonw 下 sys.stdout=None，--daemon 重定向 stdout/stderr 到 CP_DIR/loop_out.log）；API3100ControlPlane 服务经 UAC 移除，服务名保留作事件源；watchdog 缺锁分支改任务感知（有人登录且任务未运行才告警）。③Q9 wire 实证：3110 双实例拒绝（17:50:49 手动第二实例对 pid 34608，3201 alert+JSONL 落盘；注 3201 Windows 事件缺席系非提权 eventcreate best-effort，alert 文件为主通道）；3111 失租干净退出（12:18:42）；**陈旧接管**（18:00:55 lease_takeover stale=true prev_pid=34608 age_s=515→pid 41232 健康 tick）；watchdog e2e（17:42:04 heartbeat_stale「用户已登录但任务未运行」实战命中服务移除空窗+服务时代 state_stale×4+30min 去抖实测生效）；退避曲线（60→120→…→1800 封顶全程 wire）；**用户态 fetch 闭环**（SYSTEM token_missing n=1..15 vs 用户会话 17:48:36 fetch 成功→noop）。熔断/ACK 链单测级，留待自然发生或真实发布取证。④韧性：ControlPlaneLoop RestartCount=3/1min（3110 早退重试可跨 150s 陈旧窗触发接管）+WatchdogControlPlane（SYSTEM/5min）。⑤提交：ai-resource-hub c058e43（Q9 证据归档 reports/stage5_wire_evidence_20260829.md+部署日志）；计划任务/韧性配置为系统态不入 git。⑥遗留：R5（dashscope,qwen3.8-flash）拍板→解锁发布后补 ACK 链 wire；GPT 恢复后交叉复核阶段5；拔电+reboot 用户亲手验收；调优备忘（非缺陷）：退避封顶 1800s＞watchdog state_stale 阈值 20min，长退避期有告警噪音，真实发布后如扰人二选一（阈值 35min 或退避等待时刷新 state）。（2026-08-29）

- **[S-20260829-26]** :3100 价格闸门 P1 四步交付完成（纯增量、零接线，网关运行行为与今天完全一致）。①交付物：data/search_gateway/model_pricing.json 初版（10 渠道 18 条目＝13 free/3 paid/2 unknown，verified_at 错峰 08-26/08-28/08-29，free 条目全带 last_reviewed_ok，配额型 account_bound+billing_model=quota 走 7 天短窗；data/ 整目录被 .gitignore，真源在磁盘不入 git）；services/search_gateway/pricing.py 判定层（verdict 三级回退＋新鲜度降级＋M5 可撤销授权，缺文件/解析失败/结构非法一律拦、运行期不保留 last-known-good——有意推翻 channels.py:_cached_json 静默回 {} 的旧例）；pricing_group_review.py 离线核对（复用 pricing.verdict 保证与运行时同一判定无双逻辑，BLOCKED/REVIEW/EXPIRING/NOTE 四分类，只告警并追加 pricing_review.log，绝不改 unified_models.json）；P1-4 运维态与写侧（PRICING_MODE off/observe/enforce 缺省 enforce——忘配 env 只会拦多不会悄悄放；observe 必须带 ≤7 天 PRICING_OBSERVE_UNTIL，到期转 pricing_config_invalid 不静默续观；validate_startup_config 硬错误→退出码 3106（3105 已被 watchdog 占用）；write_pricing_atomic 同目录临时文件＋fsync＋回读校验＋os.replace）。②验证：tests/test_search_gateway_pricing.py 51/51（A 分类/B 新鲜度/C 加载失败/D 真源冒烟/E 核对/F 运维态写侧 13 例），路由回归 test_search_gateway_routing.py 35/35 不变；代码提交 0f3bbca（仅 3 个新文件，无现有文件改动）。③如实登记的缺口：sensetime/deepseek-v4-flash 与 dashscope/deepseek-v4-flash 是 deepseek-free 组成员但无计费证据→登记 unknown 默认拦，核对脚本真实首跑即抓出 BLOCKED=2、exit 1（防线与提前发现两条链同日闭合），P2 必须补这两款实测。④刻意取舍（已写进 pricing.py 头＋测试 B9）：新鲜度对 authorized 条目同样生效——授权回答「是否批准」、新鲜度回答「定价数据是否仍可信」，两者正交；若要豁免改 _entry_class 一处即可回退。⑤P1 明确未做：不改 api_gateway 准入序、不动 NSSM/服务配置、不注册 7 天核对计划任务（提权需另行授权），resolve_mode/effective_verdict/validate_startup_config 均为待接线能力。⑥下一步：P2（需授权）挂 pricing_gate＋NSSM env 配 observe 与到期日＋提权重启，产出组内命中数与组外 free 两份统计、补两成员实测，可顺带注册核对计划任务；P3 enforce 四条门槛 a-d；P4 扩 22± 例并纳入 run_all.py。（2026-08-29）

- **[S-20260829-25]** 阶段4正式关账：GPT Extended 终审两项必改全部落地（租约 C-20260829-04）。①终审回收：镜像 SOP（vip-19 Extended，令牌 P42-REVIEW-GATE-2026，注入 9679 字符经 ProseMirror 换行翻倍后规范化比对无损）判定**有条件关账**——判据1/2/3/5/6/8/9/10/11/12 PASS、判据4（credential resolved 语义）与判据7（expiry 影响路由缺运行态端到端）PARTIAL；D1 配对级 gating 接受（条件=未管理 pair fail-open/已管理 fail-closed 边界维持+真实发布前解决 dashscope 数据冲突）、D2 有条件（不得把"渠道名可映射"称 resolved）、D3 接受；R5 风险序 a>c>b（修正飞书 active > 显式 quarantine > 接受 local-skip）；R6 严格阶段5（scheduler/watchdog/单 writer/退避抖动/ACK 观测/重启恢复/防高速振荡）先于阶段6（webbridge 只调控制面事务，禁直接写 live/绕过 compile-validate-publish/无 identity 变更）。②必改①落地：resource_config.py credential_refs 计数 resolved/unresolved→**mapped/unmapped + semantics="channel_name_mapped"** 显式声明口径，模块头注释改写（per-key 真实 resolution 明确入阶段5），单测 test_13 锁定新键+禁 resolved 字样 16/16；重启网关（PID 36440）后 status 端点 wire 落网新口径。③必改②落地：判据7 最小 wire 演练——非生产 pair (modelscope, Qwen/Qwen2.5-72B-Instruct) 发布 active+expiry_at=2026-08-28（candidate drill-exp，validate fail-closed 通过）→ publish published+gateway_ack=true → status ACK gen=20260829T-drill-exp/1 资源 → route-plan modelscope eligible=false reason=resource_expired resource_block={reason,resource_id=res-drill-exp} → 发布空代 drill-exp-restore ACK → route-plan eligible=true resource_block=null 恢复；全程 PID 36440 不变，两跳发布各自归档前代（resource_history 含 drill-empty/drill-exp），同 PID 兼收 D2 wire 证据（drill-exp 代 credential_refs={total:1,mapped:1,unmapped:0,semantics:channel_name_mapped}）。判据7→PASS。④提交：ai-hub 50b7be6（D2 修正+单测）、ai-resource-hub e177317（评审回复+演练报告归档 reports/）、记忆仓本条；终审回复归档 docs/ai-advice/问诊回复_阶段4P42终审_有条件关账_2026-08-29.txt。⑤阶段4 关账后遗留（上线前数据准入条件，非架构缺口）：真实 candidate 发布前必须解决 (dashscope, qwen3.8-flash) 飞书数据矛盾（首选 R5-a：飞书 cap-aliyun-01 改回真实 active，资源所有者确认）；阶段5/6 未动。下一步=阶段5 设计问诊。（2026-08-29）

- **[S-20260829-24]** 阶段4 P4.2 完成：原子发布 + :3100 消费端 + 热加载 last-good（用户口头授权「你都完成啊」接手网关线 P4.2/阶段5/6 执行，租约 C-20260829-03）。①网关侧新增 resource_config.py（stat→sha 去重→解析→网关侧二次校验→immutable snapshot 指针换→last-good fail-closed；512KB rotate JSONL 日志），挂载三点：route_completion capability 检查后插配对 gating（failures outcome=resource_paused/disabled/expired/draining）、build_route_plan 增 resource_block+reason、新增 /api/resource-config/status 免鉴权只读端点供发布方 ACK 轮询；rate_limit._resolve 增 external_policy（external 优先/shadow 记 limits_shadow_differs）；capabilities.model_capabilities 增 external_capabilities（external+矩阵 100% 一致门才切换）。②控制面新增 publish.py（validate fail-closed→tmp+fsync+os.replace 原子替换→ACK 轮询超时自动回滚前代字节/无前代删残留；resource_history 保留 20 份+rollback --previous）与 sync.py --publish/--loop(60s+0-10s jitter)。③安全语义实测修正：配对级 (channel, unified_model) 精确 gating（客户端名优先、上游名兜底），credential_ref 仅计数不拒载；真实 candidate 实测发现 (dashscope, qwen3.8-flash) 与生产路由相交（cap-aliyun-01 飞书=暂停 vs 生产链在用）→真实 candidate 直接发布=生产行为变更，挂起待用户拍板；resource_config DATA_DIR 初版算错一层（services\data ≠ D:\项目\data）演练前拦截修复。④回归：网关侧新单测 16/16、控制面 publish 单测 9/9（mock 状态端点）、存量 26/26+35/35+11/11 全过（quota 套件 4/5 的 deepseek 上游 401 系既有环境问题与本改动无关，git 仅本线 5 文件）。⑤实机 12 判据演练 12/12 PASS：drill candidate=cap-aliyun-01 临时置 active 消除交集+追加 res-drill-001（xiaohongshu/dots3-note-prev active 验证配对任一 active 即放行）；零变更断言=非 active 配对∩(full=True 显式路由∪DEFAULT_CHAIN default_model)∩(enabled+key+reachable 渠道)=∅ 实测通过；dots3-note-prev 实为生产显式模型（笔记预览，xiaohongshu+zenmux 双渠道）故禁作封禁靶，改用 (modelscope, Qwen/Qwen2.5-72B-Instruct)（非显式路由、非任何 default_model、飞书本就 paused）；演练证据：发布 ACK→status 17 资源→route-plan reason=resource_paused+resource_id=cap-modelscope-01→live 注入非法 JSON 后 last-good 判定持续+fail_count=1+gen 不变→空 candidate 收尾 0 资源基线→resource_history 双代归档（drill0001+drill-empty）→全程 PID 40920 不变（一次 UAC 重启加载新代码 26480→40920）。⑥收尾态：live=20260829T-drill-empty（0 资源=无 gating 基线，等真实 candidate 拍板后发布）；代码仓 ai-hub 79d3435、资源仓 ai-resource-hub bdf0cb3。⑦下一步：GPT Extended 阶段4 终审→阶段5（资源接入流水线）/阶段6（webbridge）问诊后实施。（2026-08-29）

- **[S-20260829-23]** 价格闸门设计稿升 v0.5＝定稿（零代码改动）。用户第三轮拍板：M1-M5 五条全采纳（决策源 D-20260829-03），据此改写文档正文：§4.2 组展开由「不经价格判定」改为必须经判定并新增 B1+M1 双层防护段（含诚实记录代价：唯一改变高频行为的一条）；§4.3 observe 加 PRICING_OBSERVE_UNTIL 机器约束、authorized 加 revoked_at；§4.1 新增 M3 复核节奏量化条并把 last_reviewed_ok 写入 schema；§4.6 补写侧原子替换与运行期热载失败 pricing-invalid 两条；§2 G3 由结构性保证降为待证伪的经验断言；§5 风险表把旧 B1 行改判并新增四行（free→paid 静默翻转/observe 后门/半截 JSON/授权不可撤回）；§6 P1 交付物 4 项重排（第 4 项改为 schema 与运维态一次定完，因全是 schema 级决策拖到 P3 要再走一轮 NSSM 提权重启）、P2 observe 硬上限 7 天且必须产出组内命中数与组外 free 统计两份数据、P3 门槛由三条增至四条含组内命中为零与复核≤7 天、P4 测试由 18 例增至 22 例（新增 M1 组剔除全拒 403/M2 缺 UNTIL 拒启动与到期转异常/M4 运行期重载失败/M5 撤销回 deny）；§7 加第三轮拍板记录与执行侧保留意见；§8 追加第 6-7 步含反证结论（换 vip-19 一次成功且令牌原文引用，证实昨日 vip-12 三轮退化是实例侧故障非流程缺陷）。下一步唯一阻塞＝授权 P1 开工。（2026-08-29）

- **[S-20260829-22]** 价格闸门 v0.4 定稿的 GPT-5.6 Extended 复核第 1/3 轮成功回收（vip-19 新实例，取代昨日退化的 vip-12；本任务首次真正读到该设计的 GPT）。实读证明通过：首行原文引用令牌 PG-V04-GATE-REVIEW-20260829-731-VERIFIED。裁定＝有条件可进 P1，五条意见中两条推翻 Claude、一条抓出三方共同漏项：①反对 Claude 的 B1 只告警方案——组绕过闸门应用分层控制解决：不改组成员列表，但组展开后每个候选仍须是 fresh free 才转发，paid/unknown/stale 仅本次路由剔除、全拒则显式 403，离线脚本负责提前发现而非充当最后防线（否则 fail-closed 在调用量最大路径上名不存实亡且告警必退化为噪音）；②observe 缺机器约束，建议 PRICING_OBSERVE_UNTIL 进入即必填到期时间且窗口≤7 天，到期不静默续期而转配置异常/健康告警，需显式再批准，否则三态留下永久 fail-open 后门；③N2 的 ≤10 是人工工作量指标不是风险上界，真敞口是人工确认 free 后上游次日转 paid 而本地 JSON 剩余 29 天仍写 fresh free，故拉取器可不阻塞 P3 但人工复核节奏必须量化（建议统一≤7 天且记录最近成功复核时间）；④§4.6 只定义了启动解析失败 3106，未定义运行中 reload 解析失败——写侧须临时文件校验后 atomic replace，读侧失败不得沿用 last-known-good 静默放行，应进 pricing-invalid fail-closed 态并告警，且补进并发/热载测试；⑤authorized 只增不删必须补撤销语义（revoked_at/state=revoked），审计记录可 append-only 但授权本身必须可撤回。执行侧判断：五条均成立且 #1 是真正改变行为的一条（fast/deepseek-free 组首次受价格约束，误标条目会开始 403 最高频路径，缓解＝P2 observe 期只记不拦）。存档 docs/ai-advice/gpt56_价格闸门v0.4定稿评审{问诊,裁定}_2026-08-29.md|.txt。本轮同时暴露共享会话冲突与脚本缺 --tab，修复见 devel-tools S-20260829-03。待用户裁定是否五条全采纳升 v0.5。（2026-08-29）

- **[S-20260829-21]** 价格闸门设计稿升 v0.4＝设计定稿（零代码改动）。用户对评审 N1-N4 四条全部采纳评审推荐（决策源 D-20260829-02）：①N1 PRICING_MODE=off|observe|enforce 三态 + 条目级 authorized 双正交终态，ALLOW_PAID 退役，写 schema 前定稿；②N2 enforce 门槛三条全满足（组漂移报告无异常/观察期无误伤/组外按名直呼 free≤10 + 人工复核节奏），上游拉取器不阻塞；③N3 组白名单漂移复核周期 7 天对齐 QUOTA_STALE_DAYS，只告警不摘除；④N4 错峰登记 + 到期前 7 天预警两条都做。文档同步收口：§4.1 悬崖条款由'待 N4'改定为两条都做、Q4 漂移告警结论落到 free_models 条目；§4.2 B1 周期定 7 天；§4.3 标题标 N1 已采纳；§5 悬崖风险行改'已拍板'；§6 P1 四项交付物与 P3 门槛写死阈值 10；§7 改为'Q1-Q5+N1-N4 全部闭环，无剩余问题'（顺带补入此前只写在 §7 未回写正文的 Q4 裁定）；§8 追加第 5 步。下一步唯一阻塞项＝用户授权 P1 开工，届时按 P1→P4 分步实施、每步代码改动单独取授权。（2026-08-29）
