# 2026-09-17 工作日志

## 记忆仓每日备份同步（第 19 次执行 / 完全成功，一次成型）

- 21:02 运行 `python scripts/backup_memory.py`（managed Python 3.13.12），**耗时约 10 秒**，零失败、零重试、无超时、未触发 rebase。
- Git：工作区**干净无需提交**（今日记忆仓无其他活动）→ `pull --ff-only` 与 push **均一次成功** → **全程未用 force**。
- 一致性（三向齐备）：本地 HEAD == `origin/master` == 远端实际（`git ls-remote` **三次重试均返回同一 SHA**）== `c708722`，ahead/behind = 0/0，工作区干净，共 **338 个提交**；`087554f`（9-16）经 `merge-base --is-ancestor` 验证**仍可达**，无提交丢失。
- 备份：`D:\记忆备份\ai-hub-memory_2026-09-17_2102.zip`，**543 条目 / 2436.3 KB**（较 9-16 的 534 条目 +9）。
- 严格校验通过：testzip 无坏文件、**无重复条目**、无临时脚本 / `.old` / `__pycache__` 残留、`.git` **386 条目**（341 loose objects + 3 pack files）、核心六项（HEAD/config/packed-refs/index/`refs/heads/master`/`refs/remotes/origin/master`）齐全、解包实测 HEAD=`c708722` / `git status` **干净** / 338 提交 / `git fsck` rc=0（仅 1 个无害 dangling tree）、核心文件（AGENTS.md / STATE.md / MEMORY.json / README.md）齐全。
- 清理：0 份过期（>30 天，最老 8-28 已 **20 天**），现存 **20 份**（8-28 ~ 9-17，其中 9-07 与 9-09 各两份）+ backup.log，目录无 `.old` / `_*` 残留。
- **无漏跑日**：backup.log 显示 9-16 21:57 → 9-17 21:02 连续，调度正常（30 天内漏跑日仍为 9-08、9-13 两次）。
- 临时校验脚本 `_verify_20260917.py` 置于仓库外 `D:\记忆备份\`，用完即删，无残留。

### 沿用要点（下次执行）

1. **漏跑检测方法**：比对 backup.log 最后一条日期与当天日期，相差 >1 天即存在漏跑。本次无漏跑。
2. **不做 curl 网络探测**（假阴性）；以 `git ls-remote origin refs/heads/master` 为准，**失败后重试 ≥2 次**。
3. **rc 指纹判读表（十七次经验）**：`rc=-1` = 180s 超时；`rc=128` = Git 层拒绝或瞬时 SSL 抖动（可重试恢复）；`直接 push 成功` = 无分叉健康常态（**9-17 属此类**）；`ff-only 失败 → rebase rc=0 → push 成功` = 真实分叉；`remote rejected (cannot lock ref ...)` = 良性 ref 锁竞争，**禁止 force push**。
4. **「HEAD==origin/master」不可单独采信**：必须与 `git ls-remote` 远端实际值 + 双向 `A..B` 计数三者齐备才算同步。
5. **脚本改进建议（第十七次提出，仍未授权修改）**：`git pull --ff-only` 应拆成 `fetch` / `merge` 两步分别判错，网络类失败直接中止重试，避免把联网失败误报成分叉并空转 3 次 rebase。本次未触发该缺陷，风险仍在。
6. **两段式收尾已连续十四次有效**：先跑脚本 → 再写两份记忆文件 → **只做一次** `commit+push` + **一次**原子替换式重打包。
7. **30 天清理窗口**：最老一份 `2026-08-28` 已满 20 天，约 **9-28** 首次触发清理（距今约 11 天），属预期行为，**无需干预**；现存份数应从 20 份逐步回落，勿误判为异常删除。
