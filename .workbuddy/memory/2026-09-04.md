# 2026-09-04（周五）

## 记忆仓每日备份同步（自动化 automation-1787936803135，第 7 次执行）

- 运行 `python scripts/backup_memory.py`（managed Python 3.13.12），耗时约 20 秒，零失败零重试。
- Git：commit 1 项（9-03 收尾遗留的 automation memory.md 续写）→ `pull --ff-only` 与 push 均一次成功 → **未触发 rebase、未触发重试、未使用 force push**。
- 一致性（三方齐备）：本地 HEAD == `origin/master` == 远端实际（`git ls-remote` 核实）== `eb9ead3`，ahead/behind = 0/0，工作区干净，仓库共 318 个提交。
- 备份：`D:\记忆备份\ai-hub-memory_2026-09-04_2101.zip`，381 条目 / 2014.22 KB（首轮），严格校验通过。
- 清理：0 份过期（>30 天，最老 8-28 仅 8 天），现存 8 份（8-28 ~ 9-04）。

### 本次相对前几次的改进

1. **未做 curl 网络探测**：沿用 9-03 的修正结论——curl 访问 github.com 会给出假阴性（9-04 之前曾出现 curl `000` 但 `git ls-remote` 正常）。本次直接跑脚本，由 push 结果反证网络健康，省去探测耗时。
2. **两段式收尾一次成型**：先跑脚本（commit+push+打包）→ 再写记忆文件 → 只做一次 `commit+push` + 一次原子替换式重打包。未出现 9-02 那种「写记忆 → 备份落后 1 提交 → 反复重打包」的循环。
3. **收尾提交后仅追加一行最终 HEAD，不再重打包**：按 9-03 定稿规则，包内 HEAD 与打包时刻仓库 HEAD 严格一致即可；追加行留在工作区由次日脚本自动带走。

### 备份包校验结果（严格）

- `zipfile.testzip()` 无坏文件；无重复条目；无临时脚本残留。
- `.git` 234 条目（203 objects）；`HEAD`/`config`/`packed-refs`/`index`/`refs/heads/master`/`refs/remotes/origin/master` 六项核心文件齐全。
- 解包实测：`git log` 正常、HEAD 与仓库一致、`git status` 干净、`git fsck` rc=0（仅 1 个无害 dangling tree）、318 提交、核心文件（AGENTS.md / STATE.md / MEMORY.json / README.md）齐全。

### 沿用要点（下次执行）

1. **GitHub 可用性判据**：以 `git ls-remote origin refs/heads/master`（timeout 60）为准，**不要用 curl 访问 github.com**（9-03 已证实会假阴性）。
2. **rc 指纹判读表**：`rc=-1` = 180s 超时（网络挂起，9-01）；`rc=128` = Git 层拒绝（8-30）；本次 push 一次成功 = 健康常态。
3. **「HEAD==origin/master」不可单独采信**：必须与 `git ls-remote` 远端实际值 + 双向 `A..B` 计数三者齐备才算同步（9-02 起固化）。
4. **脚本改进建议（第五次提出，仍未授权修改）**：`git pull --ff-only` 应拆成 `fetch` / `merge` 两步分别判错，网络类失败（fetch 失败）直接中止重试，避免把联网失败误报成分叉并空转 3 次 rebase。本次未触发该缺陷，风险仍在。
5. **工程约束（Windows 环境，反复踩过）**：
   - 临时脚本放**仓库外**（`D:\记忆备份\`），否则会被 `git add -A` 误纳（9-02 踩过）。
   - 重打包临时 zip 必须与目标**同盘同目录**，跨盘 `os.replace()` 抛 WinError 17（9-02 踩过）。
   - 解包校验用 `tempfile.gettempdir()` 的 Windows 绝对路径；Git Bash 的 `/tmp` 会被 Python 拼成 `D:\tmp\`。解包根目录即 TMPX 本身，**无** `ai-hub-memory/` 子层（9-02 踩过）。
   - `git rev-parse --short HEAD origin/master`（多参数）在本环境报 `Needed a single revision`，须分开单独调用。
